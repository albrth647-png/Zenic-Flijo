"""
Conector Asana — Gestion de Tareas via Asana API
====================================================

Permite gestionar tareas, proyectos, secciones y etiquetas
en Asana via la REST API usando HttpClient.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁAsanaConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁ_create_task__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁ_get_task__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁ_list_tasks__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁ_update_task__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁ_list_projects__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAsanaConnectorǁ_create_project__mutmut: MutantDict = {}  # type: ignore


class AsanaConnector(BaseConnector):
    """Conector para Asana: tareas, proyectos y secciones."""

    name = "asana"
    version = "1.0.0"
    description = "Gestiona tareas, proyectos y secciones en Asana"
    category = "project_management"
    icon = "list"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://app.asana.com/api/1.0"
        self._http: HttpClient | None = None

    def xǁAsanaConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://app.asana.com/api/1.0"
        self._http: HttpClient | None = None

    def xǁAsanaConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁAsanaConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://app.asana.com/api/1.0XX"
        self._http: HttpClient | None = None

    def xǁAsanaConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://APP.ASANA.COM/API/1.0"
        self._http: HttpClient | None = None

    def xǁAsanaConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://app.asana.com/api/1.0"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API de Asana."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXAsanaConnector: Personal Access Token no configuradoXX")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("asanaconnector: personal access token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ASANACONNECTOR: PERSONAL ACCESS TOKEN NO CONFIGURADO")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return True

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = None
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") and getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(None, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, None, "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", None) or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr("_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", ) or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "XX_api_keyXX", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_API_KEY", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "XXXX") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(None, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, None, "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", None)
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr("_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", )
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "XX_tokenXX", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_TOKEN", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "XXXX")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = None
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth(None)
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"XXheadersXX": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"HEADERS": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = None

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace(None, "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", None)

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", )

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get(None, "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", None).replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", ).replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get(None, {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", None).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get({}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", ).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("XXheadersXX", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("HEADERS", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("XXAuthorizationXX", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("AUTHORIZATION", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "XXXX").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("XXBearer XX", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("BEARER ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "XXXX")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = None
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=None,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(None, token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=None)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", )

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("XXBearerXX", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("BEARER", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = None
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get(None)
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("XX/users/meXX")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/USERS/ME")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code != 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 402:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error(None)
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("XXAsanaConnector: PAT invalido (401)XX")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("asanaconnector: pat invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("ASANACONNECTOR: PAT INVALIDO (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return True
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(None)

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = None
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = False
        self._log_operation("connect", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation(None, "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", None)
        return True

    def xǁAsanaConnectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", )
        return True

    def xǁAsanaConnectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("XXconnectXX", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("CONNECT", "PAT configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "XXPAT configuradoXX")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "pat configurado")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT CONFIGURADO")
        return True

    def xǁAsanaConnectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con la API de Asana."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AsanaConnector: Personal Access Token no configurado")
            return False

        # Extract PAT from auth provider
        pat = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "_token", "")
        if not pat:
            auth_request = self._auth_provider.apply_auth({"headers": {}})
            pat = auth_request.get("headers", {}).get("Authorization", "").replace("Bearer ", "")

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=pat)

        # Validate credentials
        try:
            response = self._http.get("/users/me")
            if response.status_code == 401:
                logger.error("AsanaConnector: PAT invalido (401)")
                return False
        except HTTPClientError as e:
            logger.warning(f"AsanaConnector: error validando credenciales: {e}")

        self._connected = True
        self._log_operation("connect", "PAT configurado")
        return False

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXcreate_taskXX": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "CREATE_TASK": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "XXget_taskXX": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "GET_TASK": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "XXlist_tasksXX": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "LIST_TASKS": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "XXupdate_taskXX": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "UPDATE_TASK": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "XXlist_projectsXX": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "LIST_PROJECTS": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "XXcreate_projectXX": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "CREATE_PROJECT": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁAsanaConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Asana.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_task": self._create_task,
            "get_task": self._get_task,
            "list_tasks": self._list_tasks,
            "update_task": self._update_task,
            "list_projects": self._list_projects,
            "create_project": self._create_project,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que el PAT de Asana este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁAsanaConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que el PAT de Asana este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁAsanaConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que el PAT de Asana este configurado."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁAsanaConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que el PAT de Asana este configurado."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con Asana."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAsanaConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con Asana."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAsanaConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con Asana."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAsanaConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con Asana."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁAsanaConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con Asana."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁAsanaConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con Asana."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁAsanaConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con Asana."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁAsanaConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con Asana."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁAsanaConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con Asana."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁ_create_task__mutmut)
    def _create_task(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = None
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get(None, "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", None)
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", )
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("XXnameXX", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("NAME", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "XXXX")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"XXsuccessXX": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"SUCCESS": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": True, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "XXerrorXX": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "ERROR": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "XXParametro requerido: nameXX"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "PARAMETRO REQUERIDO: NAME"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation(None, f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", None)

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation(f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", )

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("XXcreate_taskXX", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("CREATE_TASK", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = None
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"XXnameXX": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"NAME": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get(None):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("XXprojectsXX"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("PROJECTS"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = None
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["XXprojectsXX"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["PROJECTS"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["XXprojectsXX"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["PROJECTS"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get(None):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("XXnotesXX"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("NOTES"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = None
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["XXnotesXX"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["NOTES"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["XXnotesXX"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["NOTES"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get(None):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("XXassigneeXX"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("ASSIGNEE"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = None
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["XXassigneeXX"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["ASSIGNEE"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["XXassigneeXX"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["ASSIGNEE"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get(None):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("XXdue_onXX"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("DUE_ON"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = None
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["XXdue_onXX"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["DUE_ON"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["XXdue_onXX"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["DUE_ON"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get(None):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("XXdue_atXX"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("DUE_AT"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = None
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["XXdue_atXX"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["DUE_AT"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["XXdue_atXX"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["DUE_AT"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = None
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get(None, {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", None)
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get({})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", )
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("XXdataXX", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("DATA", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(None)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = None
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post(None, json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json=None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post(json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", )
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("XX/tasksXX", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/TASKS", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"XXdataXX": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"DATA": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = None
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = None
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get(None, {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", None)
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get({})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", )
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("XXdataXX", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("DATA", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "XXsuccessXX": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "SUCCESS": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": False,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "XXgidXX": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "GID": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get(None, ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", None),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get(""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("XXgidXX", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("GID", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", "XXXX"),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "XXnameXX": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "NAME": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get(None, name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get(name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", ),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("XXnameXX", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("NAME", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁAsanaConnectorǁ_create_task__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una tarea en Asana.

        Args:
            params: Debe contener 'name' y 'projects' (lista de IDs), opcionalmente 'notes', 'assignee'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}
        self._log_operation("create_task", f"name={name}")

        try:
            # Build task data — Asana uses 'data' wrapper
            task_data: dict[str, Any] = {"name": name}
            if params.get("projects"):
                task_data["projects"] = params["projects"]
            if params.get("notes"):
                task_data["notes"] = params["notes"]
            if params.get("assignee"):
                task_data["assignee"] = params["assignee"]
            if params.get("due_on"):
                task_data["due_on"] = params["due_on"]
            if params.get("due_at"):
                task_data["due_at"] = params["due_at"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                task_data.update(extra)

            response = self._http.post("/tasks", json={"data": task_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ""),
                "name": task.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁ_get_task__mutmut)
    def _get_task(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = None
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get(None, "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", None)
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", )
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("XXtask_gidXX", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("TASK_GID", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "XXXX")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"XXsuccessXX": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"SUCCESS": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": True, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "XXerrorXX": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "ERROR": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "XXParametro requerido: task_gidXX"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "PARAMETRO REQUERIDO: TASK_GID"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation(None, f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", None)

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation(f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", )

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("XXget_taskXX", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("GET_TASK", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = ""
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = None
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get(None, "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", None)
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", )
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("XXopt_fieldsXX", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("OPT_FIELDS", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "XXXX")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = None

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"XXopt_fieldsXX": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"OPT_FIELDS": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = None
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(None, params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", )
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = None
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = None
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get(None, {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", None)
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get({})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", )
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("XXdataXX", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("DATA", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "XXsuccessXX": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "SUCCESS": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": False,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "XXgidXX": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "GID": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get(None, task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", None),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get(task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("XXgidXX", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("GID", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "XXnameXX": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "NAME": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get(None, ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", None),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get(""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("XXnameXX", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("NAME", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", "XXXX"),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "XXcompletedXX": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "COMPLETED": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get(None, False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", None),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get(False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", ),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("XXcompletedXX", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("COMPLETED", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", True),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "XXdataXX": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "DATA": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁAsanaConnectorǁ_get_task__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene una tarea por su GID.

        Args:
            params: Debe contener 'task_gid'
        """
        task_gid = params.get("task_gid", "")
        if not task_gid:
            return {"success": False, "error": "Parametro requerido: task_gid"}
        self._log_operation("get_task", f"gid={task_gid}")

        try:
            query_params: dict[str, Any] | None = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params = {"opt_fields": opt_fields}

            response = self._http.get(f"/tasks/{task_gid}", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "name": task.get("name", ""),
                "completed": task.get("completed", False),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁ_list_tasks__mutmut)
    def _list_tasks(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = None
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get(None, "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", None)
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", )
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("XXproject_gidXX", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("PROJECT_GID", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "XXXX")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = None
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get(None, "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", None)
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", )
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("XXworkspace_gidXX", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("WORKSPACE_GID", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "XXXX")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = None
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get(None, "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", None)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", )
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("XXassignee_gidXX", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("ASSIGNEE_GID", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "XXXX")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = None
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get(None, 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", None)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get(20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", )
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("XXlimitXX", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("LIMIT", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 21)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = None
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get(None, "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", None)
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", )
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("XXoffsetXX", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("OFFSET", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "XXXX")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation(None, f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", None)

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation(f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", )

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("XXlist_tasksXX", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("LIST_TASKS", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = None
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"XXlimitXX": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"LIMIT": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = None
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["XXoffsetXX"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["OFFSET"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = None
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get(None, "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", None)
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", )
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("XXopt_fieldsXX", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("OPT_FIELDS", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "XXXX")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = None
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["XXopt_fieldsXX"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["OPT_FIELDS"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = None
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(None, params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=None)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", )
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid or assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = None
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "XXworkspaceXX": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "WORKSPACE": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "XXassigneeXX": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "ASSIGNEE": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "XXlimitXX": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "LIMIT": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = None
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["XXoffsetXX"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["OFFSET"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = None
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get(None, params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=None)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get(params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", )
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("XX/tasksXX", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/TASKS", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"XXsuccessXX": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"SUCCESS": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": True, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "XXerrorXX": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "ERROR": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "XXParametros requeridos: project_gid o (workspace_gid + assignee_gid)XX"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "PARAMETROS REQUERIDOS: PROJECT_GID O (WORKSPACE_GID + ASSIGNEE_GID)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = None
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "XXsuccessXX": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "SUCCESS": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": False,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "XXdataXX": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "DATA": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get(None, []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", None),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get([]),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", ),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("XXdataXX", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("DATA", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "XXnext_pageXX": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "NEXT_PAGE": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get(None, None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get(None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", ),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("XXnext_pageXX", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("NEXT_PAGE", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁAsanaConnectorǁ_list_tasks__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista tareas de un proyecto o workspace.

        Args:
            params: Debe contener 'project_gid' o 'workspace_gid'
        """
        project_gid = params.get("project_gid", "")
        workspace_gid = params.get("workspace_gid", "")
        assignee_gid = params.get("assignee_gid", "")
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_tasks", f"project={project_gid}")

        try:
            if project_gid:
                # List tasks in a project
                query_params: dict[str, Any] = {"limit": limit}
                if offset:
                    query_params["offset"] = offset
                opt_fields = params.get("opt_fields", "")
                if opt_fields:
                    query_params["opt_fields"] = opt_fields
                response = self._http.get(f"/projects/{project_gid}/tasks", params=query_params)
            elif workspace_gid and assignee_gid:
                # List tasks assigned to a user in a workspace
                query_params = {
                    "workspace": workspace_gid,
                    "assignee": assignee_gid,
                    "limit": limit,
                }
                if offset:
                    query_params["offset"] = offset
                response = self._http.get("/tasks", params=query_params)
            else:
                return {"success": False, "error": "Parametros requeridos: project_gid o (workspace_gid + assignee_gid)"}

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁ_update_task__mutmut)
    def _update_task(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = None
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get(None, "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", None)
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", )
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("XXtask_gidXX", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("TASK_GID", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "XXXX")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = None
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get(None, {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", None)
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get({})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", )
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("XXfieldsXX", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("FIELDS", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid and not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"SUCCESS": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": True, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "XXerrorXX": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "ERROR": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "XXParametros requeridos: task_gid, fieldsXX"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: TASK_GID, FIELDS"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation(None, f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", None)

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation(f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", )

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("XXupdate_taskXX", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("UPDATE_TASK", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = None
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(None, json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json=None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", )
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"XXdataXX": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"DATA": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = None
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = None
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get(None, {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", None)
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get({})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", )
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("XXdataXX", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("DATA", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "XXsuccessXX": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "SUCCESS": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": False,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "XXgidXX": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "GID": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get(None, task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", None),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get(task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", ),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("XXgidXX", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("GID", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "XXdataXX": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "DATA": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁAsanaConnectorǁ_update_task__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Actualiza una tarea en Asana.

        Args:
            params: Debe contener 'task_gid' y 'fields' (dict de campos)
        """
        task_gid = params.get("task_gid", "")
        fields = params.get("fields", {})
        if not task_gid or not fields:
            return {"success": False, "error": "Parametros requeridos: task_gid, fields"}
        self._log_operation("update_task", f"gid={task_gid}")

        try:
            response = self._http.put(f"/tasks/{task_gid}", json={"data": fields})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            task = result.get("data", {})
            return {
                "success": True,
                "gid": task.get("gid", task_gid),
                "data": task,
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁ_list_projects__mutmut)
    def _list_projects(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = None
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get(None, "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", None)
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", )
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("XXworkspace_gidXX", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("WORKSPACE_GID", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "XXXX")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = None
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get(None, False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", None)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get(False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", )
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("XXarchivedXX", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("ARCHIVED", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", True)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = None
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get(None, 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", None)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get(20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", )
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("XXlimitXX", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("LIMIT", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 21)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = None
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get(None, "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", None)
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", )
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("XXoffsetXX", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("OFFSET", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "XXXX")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation(None)

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("XXlist_projectsXX")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("LIST_PROJECTS")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = None
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "XXworkspaceXX": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "WORKSPACE": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "XXarchivedXX": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "ARCHIVED": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).upper(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(None).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "XXlimitXX": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "LIMIT": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = None
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["XXoffsetXX"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["OFFSET"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = None
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get(None, "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", None)
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", )
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("XXopt_fieldsXX", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("OPT_FIELDS", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "XXXX")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = None

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["XXopt_fieldsXX"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["OPT_FIELDS"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = None
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get(None, params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get(params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", )
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("XX/projectsXX", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/PROJECTS", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = None
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "XXsuccessXX": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "SUCCESS": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": False,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "XXdataXX": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "DATA": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get(None, []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", None),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get([]),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", ),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("XXdataXX", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("DATA", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "XXnext_pageXX": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "NEXT_PAGE": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get(None, None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get(None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", ),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("XXnext_pageXX", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("NEXT_PAGE", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁAsanaConnectorǁ_list_projects__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos del workspace.

        Args:
            params: Debe contener 'workspace_gid' y opcionalmente 'archived'
        """
        workspace_gid = params.get("workspace_gid", "")
        archived = params.get("archived", False)
        limit = params.get("limit", 20)
        offset = params.get("offset", "")
        self._log_operation("list_projects")

        try:
            query_params: dict[str, Any] = {
                "workspace": workspace_gid,
                "archived": str(archived).lower(),
                "limit": limit,
            }
            if offset:
                query_params["offset"] = offset
            opt_fields = params.get("opt_fields", "")
            if opt_fields:
                query_params["opt_fields"] = opt_fields

            response = self._http.get("/projects", params=query_params)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            return {
                "success": True,
                "data": result.get("data", []),
                "next_page": result.get("next_page", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAsanaConnectorǁ_create_project__mutmut)
    def _create_project(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = None
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get(None, "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", None)
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", )
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("XXnameXX", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("NAME", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "XXXX")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = None
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get(None, "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", None)
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", )
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("XXworkspace_gidXX", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("WORKSPACE_GID", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "XXXX")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name and not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"SUCCESS": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": True, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "XXerrorXX": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "ERROR": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "XXParametros requeridos: name, workspace_gidXX"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: NAME, WORKSPACE_GID"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation(None, f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", None)

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation(f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", )

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("XXcreate_projectXX", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("CREATE_PROJECT", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = None
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "XXnameXX": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "NAME": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "XXworkspaceXX": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "WORKSPACE": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get(None):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("XXcolorXX"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("COLOR"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = None
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["XXcolorXX"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["COLOR"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["XXcolorXX"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["COLOR"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get(None):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("XXnotesXX"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("NOTES"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = None
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["XXnotesXX"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["NOTES"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["XXnotesXX"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["NOTES"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get(None):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("XXteamXX"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("TEAM"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = None
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["XXteamXX"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["TEAM"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["XXteamXX"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["TEAM"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = None
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get(None, {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", None)
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get({})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", )
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("XXdataXX", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("DATA", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(None)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = None
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post(None, json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json=None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post(json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", )
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("XX/projectsXX", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/PROJECTS", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"XXdataXX": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"DATA": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = None
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = None
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get(None, {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", None)
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get({})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", )
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("XXdataXX", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("DATA", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "XXsuccessXX": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "SUCCESS": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": False,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "XXgidXX": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "GID": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get(None, ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", None),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get(""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("XXgidXX", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("GID", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", "XXXX"),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "XXnameXX": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "NAME": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get(None, name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get(name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", ),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("XXnameXX", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("NAME", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁAsanaConnectorǁ_create_project__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un proyecto en Asana.

        Args:
            params: Debe contener 'name', 'workspace_gid' y opcionalmente 'color', 'notes'
        """
        name = params.get("name", "")
        workspace_gid = params.get("workspace_gid", "")
        if not name or not workspace_gid:
            return {"success": False, "error": "Parametros requeridos: name, workspace_gid"}
        self._log_operation("create_project", f"name={name}")

        try:
            project_data: dict[str, Any] = {
                "name": name,
                "workspace": workspace_gid,
            }
            if params.get("color"):
                project_data["color"] = params["color"]
            if params.get("notes"):
                project_data["notes"] = params["notes"]
            if params.get("team"):
                project_data["team"] = params["team"]
            # Allow any additional fields
            extra = params.get("data", {})
            if isinstance(extra, dict):
                project_data.update(extra)

            response = self._http.post("/projects", json={"data": project_data})
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            result = response.json()
            project = result.get("data", {})
            return {
                "success": True,
                "gid": project.get("gid", ""),
                "name": project.get("name", name),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

mutants_xǁAsanaConnectorǁ__init____mutmut['_mutmut_orig'] = AsanaConnector.xǁAsanaConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ__init____mutmut['xǁAsanaConnectorǁ__init____mutmut_1'] = AsanaConnector.xǁAsanaConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ
] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_70'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_71'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_72'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_73'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_74'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_75'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_76'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_77'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_78'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_79'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_80'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_81'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_82'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_83'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_84'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_85'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_86'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_87'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_88'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_89'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_90'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_91'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_92'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_93'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_94'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_95'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_list_projects__mutmut['xǁAsanaConnectorǁ_list_projects__mutmut_96'] = AsanaConnector.xǁAsanaConnectorǁ_list_projects__mutmut_96 # type: ignore # mutmut generated

mutants_xǁAsanaConnectorǁ_create_project__mutmut['_mutmut_orig'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_1'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_2'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_3'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_4'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_5'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_6'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_7'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_8'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_9'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_10'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_11'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_12'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_13'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_14'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_15'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_16'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_17'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_18'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_19'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_20'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_21'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_22'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_23'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_24'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_25'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_26'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_27'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_28'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_29'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_30'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_31'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_32'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_33'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_34'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_35'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_36'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_37'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_38'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_39'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_40'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_41'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_42'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_43'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_44'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_45'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_46'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_47'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_48'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_49'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_50'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_51'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_52'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_53'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_54'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_55'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_56'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_57'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_58'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_59'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_60'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_61'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_62'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_63'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_64'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_65'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_66'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_67'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_68'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_69'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_70'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_71'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_72'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_73'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_74'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_75'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_76'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_77'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_78'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_79'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_80'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_81'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_82'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_83'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_84'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_85'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_86'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_87'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_88'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_89'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_90'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_91'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_92'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_93'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_94'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_95'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_96'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_96 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_97'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_97 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_98'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_98 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_99'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_99 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_100'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_100 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_101'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_101 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_102'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_102 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_103'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_103 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_104'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_104 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_105'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_105 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_106'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_106 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_107'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_107 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_108'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_108 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_109'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_109 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_110'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_110 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_111'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_111 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_112'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_112 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_113'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_113 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_114'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_114 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_115'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_115 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_116'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_116 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_117'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_117 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_118'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_118 # type: ignore # mutmut generated
mutants_xǁAsanaConnectorǁ_create_project__mutmut['xǁAsanaConnectorǁ_create_project__mutmut_119'] = AsanaConnector.xǁAsanaConnectorǁ_create_project__mutmut_119 # type: ignore # mutmut generated


ASANA_SCHEMA = ConnectorSchema(
    name="asana",
    version="1.0.0",
    description="Gestiona tareas, proyectos y secciones en Asana",
    category="project_management",
    icon="list",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="create_task", description="Crea una tarea", category="write"),
        ActionDefinition(name="get_task", description="Obtiene una tarea", category="read"),
        ActionDefinition(name="list_tasks", description="Lista tareas", category="read"),
        ActionDefinition(name="update_task", description="Actualiza una tarea", category="write"),
        ActionDefinition(name="list_projects", description="Lista proyectos", category="read"),
        ActionDefinition(name="create_project", description="Crea un proyecto", category="write"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="api_key", required_fields=["personal_access_token"], description="Asana Personal Access Token")
    ],
)
