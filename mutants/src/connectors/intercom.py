"""
Conector Intercom — Mensajeria de Atencion al Cliente
=======================================================

Permite enviar mensajes a usuarios, gestionar conversaciones,
crear contactos y administrar tickets via la API de Intercom.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁIntercomConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁ_send_message__mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁ_list_conversations__mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁ_get_conversation__mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁ_create_contact__mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁ_list_contacts__mutmut: MutantDict = {}  # type: ignore
mutants_xǁIntercomConnectorǁ_create_ticket__mutmut: MutantDict = {}  # type: ignore


class IntercomConnector(BaseConnector):
    """Conector para Intercom: mensajeria y atencion al cliente."""

    name = "intercom"
    version = "1.0.0"
    description = "Gestiona conversaciones, contactos y tickets via Intercom"
    category = "communication"
    icon = "headphones"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.intercom.io"
        self._access_token: str = ""
        self._http: HttpClient | None = None

    def xǁIntercomConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.intercom.io"
        self._access_token: str = ""
        self._http: HttpClient | None = None

    def xǁIntercomConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._access_token: str = ""
        self._http: HttpClient | None = None

    def xǁIntercomConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://api.intercom.ioXX"
        self._access_token: str = ""
        self._http: HttpClient | None = None

    def xǁIntercomConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://API.INTERCOM.IO"
        self._access_token: str = ""
        self._http: HttpClient | None = None

    def xǁIntercomConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.intercom.io"
        self._access_token: str = None
        self._http: HttpClient | None = None

    def xǁIntercomConnectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.intercom.io"
        self._access_token: str = "XXXX"
        self._http: HttpClient | None = None

    def xǁIntercomConnectorǁ__init____mutmut_6(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.intercom.io"
        self._access_token: str = ""
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXIntercomConnector: Access Token no configuradoXX")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("intercomconnector: access token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("INTERCOMCONNECTOR: ACCESS TOKEN NO CONFIGURADO")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return True

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = None

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(None, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, None, "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", None)

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr("_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", )

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "XX_api_keyXX", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_API_KEY", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "XXXX")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token or hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(None, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, None):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr("_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, ):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "XX_credentialsXX"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_CREDENTIALS"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = None

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get(None, "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", None)

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", )

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("XXaccess_tokenXX", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("ACCESS_TOKEN", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "XXXX")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error(None)
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("XXIntercomConnector: access_token es requeridoXX")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("intercomconnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("INTERCOMCONNECTOR: ACCESS_TOKEN ES REQUERIDO")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return True

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = None
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=None,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(None, token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=None)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", )
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("XXBearerXX", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("BEARER", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header(None, "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", None)
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", )
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("XXIntercom-VersionXX", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("intercom-version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("INTERCOM-VERSION", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "XX2.11XX")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header(None, "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", None)

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", )

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("XXAcceptXX", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("ACCEPT", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "XXapplication/jsonXX")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "APPLICATION/JSON")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = None
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = False
        self._log_operation("connect", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation(None, "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", None)
        return True

    def xǁIntercomConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", )
        return True

    def xǁIntercomConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("XXconnectXX", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("CONNECT", "Access Token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "XXAccess Token configuradoXX")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "access token configurado")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "ACCESS TOKEN CONFIGURADO")
        return True

    def xǁIntercomConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con la API de Intercom."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("IntercomConnector: Access Token no configurado")
            return False

        self._access_token = getattr(self._auth_provider, "_api_key", "")

        # Try to get from credentials dict if available
        if not self._access_token and hasattr(self._auth_provider, "_credentials"):
            self._access_token = self._auth_provider._credentials.get("access_token", "")

        if not self._access_token:
            logger.error("IntercomConnector: access_token es requerido")
            return False

        # Set up HttpClient with Bearer auth (Intercom uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._access_token)
        # Intercom requires Intercom-Version header
        self._http.set_header("Intercom-Version", "2.11")
        # Override Accept header for Intercom
        self._http.set_header("Accept", "application/json")

        self._connected = True
        self._log_operation("connect", "Access Token configurado")
        return False

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXsend_messageXX": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "SEND_MESSAGE": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "XXlist_conversationsXX": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "LIST_CONVERSATIONS": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "XXget_conversationXX": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "GET_CONVERSATION": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "XXcreate_contactXX": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "CREATE_CONTACT": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "XXlist_contactsXX": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "LIST_CONTACTS": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "XXcreate_ticketXX": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "CREATE_TICKET": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁIntercomConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Intercom.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "list_conversations": self._list_conversations,
            "get_conversation": self._get_conversation,
            "create_contact": self._create_contact,
            "list_contacts": self._list_contacts,
            "create_ticket": self._create_ticket,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que el Access Token de Intercom este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁIntercomConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que el Access Token de Intercom este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁIntercomConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que el Access Token de Intercom este configurado."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁIntercomConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que el Access Token de Intercom este configurado."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = False
        self._access_token = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = False
        self._access_token = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = None
        self._access_token = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = True
        self._access_token = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = False
        self._access_token = None
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = False
        self._access_token = "XXXX"
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = False
        self._access_token = ""
        self._http = ""
        self._log_operation("disconnect")
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = False
        self._access_token = ""
        self._http = None
        self._log_operation(None)
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = False
        self._access_token = ""
        self._http = None
        self._log_operation("XXdisconnectXX")
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_8(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = False
        self._access_token = ""
        self._http = None
        self._log_operation("DISCONNECT")
        return True

    def xǁIntercomConnectorǁdisconnect__mutmut_9(self) -> bool:
        """Cierra la conexion con Intercom."""
        self._connected = False
        self._access_token = ""
        self._http = None
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁ_send_message__mutmut)
    def _send_message(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = None
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get(None, "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", None)
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", )
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("XXconversation_idXX", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("CONVERSATION_ID", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "XXXX")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = None
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get(None, "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", None)
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", )
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("XXuser_idXX", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("USER_ID", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "XXXX")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = None
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get(None, "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", None)
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", )
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("XXbodyXX", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("BODY", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "XXXX")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = None

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get(None, "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", None)

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", )

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("XXmessage_typeXX", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("MESSAGE_TYPE", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "XXcommentXX")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "COMMENT")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body and (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id or not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"XXsuccessXX": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"SUCCESS": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": True, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "XXerrorXX": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "ERROR": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "XXRequiere conversation_id o user_id, y bodyXX"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "REQUIERE CONVERSATION_ID O USER_ID, Y BODY"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation(None, f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation(f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("XXsend_messageXX", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("SEND_MESSAGE", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id and user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = None
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "XXmessage_typeXX": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "MESSAGE_TYPE": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "XXbodyXX": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "BODY": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "XXtypeXX": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "TYPE": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "XXadminXX",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "ADMIN",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get(None):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("XXadmin_idXX"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("ADMIN_ID"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = None

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["XXadmin_idXX"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["ADMIN_ID"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["XXadmin_idXX"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["ADMIN_ID"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = None
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    None,
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=None,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = None
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "XXfromXX": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "FROM": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"XXtypeXX": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"TYPE": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "XXadminXX", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "ADMIN", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "XXidXX": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "ID": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get(None, "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", None)},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", )},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("XXadmin_idXX", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("ADMIN_ID", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "XXXX")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "XXtoXX": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "TO": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"XXtypeXX": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"TYPE": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "XXuserXX", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "USER", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "XXidXX": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "ID": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "XXbodyXX": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "BODY": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "XXmessage_typeXX": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "MESSAGE_TYPE": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "XXinappXX",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "INAPP",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get(None):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("XXsubjectXX"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("SUBJECT"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = None

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["XXsubjectXX"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["SUBJECT"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["XXsubjectXX"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["SUBJECT"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = None

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    None,
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=None,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "XX/messagesXX",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/MESSAGES",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = None
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() and {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "XXsuccessXX": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "SUCCESS": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": False,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "XXmessage_idXX": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "MESSAGE_ID": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get(None, ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", None),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get(""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("XXidXX", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("ID", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", "XXXX"),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "XXconversation_idXX": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "CONVERSATION_ID": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get(None, conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", None),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get(conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", ),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("XXconversation_idXX", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("CONVERSATION_ID", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "XXbodyXX": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "BODY": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get(None, body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", None),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get(body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", ),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("XXbodyXX", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("BODY", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "XXtypeXX": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "TYPE": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get(None, ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", None),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get(""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("XXtypeXX", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("TYPE", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", "XXXX"),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "XXcreated_atXX": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "CREATED_AT": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get(None, ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", None),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get(""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("XXcreated_atXX", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("CREATED_AT", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", "XXXX"),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "XXerror_codeXX": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "ERROR_CODE": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get(None, "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", None) if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_206(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_207(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", ) if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_208(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[1].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_209(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("XXcodeXX", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_210(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("CODE", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_211(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "XXXX") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_212(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "XXXX",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_213(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_214(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_215(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_216(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_217(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_218(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_219(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_220(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_221(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_send_message__mutmut_222(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje a un usuario o conversacion de Intercom.

        Args:
            params: Debe contener 'conversation_id' o 'user_id', y 'body'
        """
        conversation_id = params.get("conversation_id", "")
        user_id = params.get("user_id", "")
        body = params.get("body", "")
        message_type = params.get("message_type", "comment")

        if not body or (not conversation_id and not user_id):
            return {"success": False, "error": "Requiere conversation_id o user_id, y body"}

        self._log_operation("send_message", f"conversation={conversation_id or user_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            if conversation_id:
                # Reply to existing conversation
                payload: dict[str, Any] = {
                    "message_type": message_type,
                    "body": body,
                    "type": "admin",
                }
                if params.get("admin_id"):
                    payload["admin_id"] = params["admin_id"]

                response = self._http.post(
                    f"/conversations/{conversation_id}/reply",
                    json=payload,
                )
            else:
                # Start a new conversation with a user
                payload = {
                    "from": {"type": "admin", "id": params.get("admin_id", "")},
                    "to": {"type": "user", "id": user_id},
                    "body": body,
                    "message_type": "inapp",
                }
                if params.get("subject"):
                    payload["subject"] = params["subject"]

                response = self._http.post(
                    "/messages",
                    json=payload,
                )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "conversation_id": data.get("conversation_id", conversation_id),
                    "body": data.get("body", body),
                    "type": data.get("type", ""),
                    "created_at": data.get("created_at", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁ_list_conversations__mutmut)
    def _list_conversations(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = None
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get(None, 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", None)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get(20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", )
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("XXlimitXX", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("LIMIT", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 21)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation(None, f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation(f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("XXlist_conversationsXX", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("LIST_CONVERSATIONS", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = None
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"XXper_pageXX": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"PER_PAGE": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(None, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, None)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, )}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 61)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get(None):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("XXstatusXX"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("STATUS"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = None
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["XXstatusXX"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["STATUS"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["XXstatusXX"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["STATUS"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get(None):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("XXassignee_idXX"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("ASSIGNEE_ID"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = None
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["XXassignee_idXX"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["ASSIGNEE_ID"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["XXassignee_idXX"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["ASSIGNEE_ID"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get(None):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("XXstart_afterXX"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("START_AFTER"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = None
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["XXstarting_afterXX"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["STARTING_AFTER"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["XXstart_afterXX"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["START_AFTER"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get(None):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("XXcreated_afterXX"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("CREATED_AFTER"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = None

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["XXcreated_at_afterXX"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["CREATED_AT_AFTER"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["XXcreated_afterXX"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["CREATED_AFTER"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = None

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get(None, params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=None)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get(params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", )

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("XX/conversationsXX", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/CONVERSATIONS", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = None
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() and {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = None
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get(None, [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", None)
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get([])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", )
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("XXconversationsXX", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("CONVERSATIONS", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "XXsuccessXX": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "SUCCESS": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": False,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "XXconversationsXX": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "CONVERSATIONS": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "XXidXX": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "ID": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get(None, ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", None),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get(""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("XXidXX", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("ID", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", "XXXX"),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "XXcreated_atXX": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "CREATED_AT": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get(None, ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", None),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get(""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("XXcreated_atXX", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("CREATED_AT", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", "XXXX"),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "XXupdated_atXX": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "UPDATED_AT": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get(None, ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", None),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get(""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("XXupdated_atXX", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("UPDATED_AT", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", "XXXX"),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "XXstateXX": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "STATE": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get(None, ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", None),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get(""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("XXstateXX", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("STATE", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", "XXXX"),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "XXopenXX": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "OPEN": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get(None, False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", None),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get(False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", ),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("XXopenXX", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("OPEN", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", True),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "XXreadXX": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "READ": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get(None, False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", None),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get(False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", ),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("XXreadXX", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("READ", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", True),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "XXpriorityXX": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "PRIORITY": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get(None, ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", None),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get(""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("XXpriorityXX", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("PRIORITY", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", "XXXX"),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "XXsubjectXX": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "SUBJECT": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get(None, ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", None),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get(""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("XXtitleXX", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("TITLE", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", "XXXX"),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "XXsourceXX": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "SOURCE": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get(None, {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", None),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get({}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", ),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("XXsourceXX", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("SOURCE", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "XXassigneeXX": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "ASSIGNEE": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get(None, {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", None),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get({}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", ),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("XXassigneeXX", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("ASSIGNEE", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "XXcontactsXX": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "CONTACTS": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get(None, {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", None),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get({}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", ),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("XXcontactsXX", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("CONTACTS", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "XXtotalXX": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "TOTAL": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "XXpagesXX": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "PAGES": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get(None, {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", None),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get({}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", ),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("XXpagesXX", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("PAGES", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_206(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_207(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_208(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_209(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_210(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_211(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_212(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_213(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_214(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_215(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_216(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_217(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_218(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_219(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_220(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_221(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_222(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_223(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_conversations__mutmut_224(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista conversaciones de Intercom.

        Args:
            params: Opcionalmente 'limit', 'status' y 'assignee_id'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_conversations", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("status"):
                query_params["status"] = params["status"]
            if params.get("assignee_id"):
                query_params["assignee_id"] = params["assignee_id"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]

            response = self._http.get("/conversations", params=query_params)

            if response.ok:
                data = response.json() or {}
                conversations = data.get("conversations", [])
                return {
                    "success": True,
                    "conversations": [
                        {
                            "id": conv.get("id", ""),
                            "created_at": conv.get("created_at", ""),
                            "updated_at": conv.get("updated_at", ""),
                            "state": conv.get("state", ""),
                            "open": conv.get("open", False),
                            "read": conv.get("read", False),
                            "priority": conv.get("priority", ""),
                            "subject": conv.get("title", ""),
                            "source": conv.get("source", {}),
                            "assignee": conv.get("assignee", {}),
                            "contacts": conv.get("contacts", {}),
                        }
                        for conv in conversations
                    ],
                    "total": len(conversations),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁ_get_conversation__mutmut)
    def _get_conversation(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = None
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get(None, "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", None)
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", )
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("XXconversation_idXX", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("CONVERSATION_ID", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "XXXX")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"SUCCESS": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": True, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "ERROR": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "XXParametro requerido: conversation_idXX"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: CONVERSATION_ID"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation(None, f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation(f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("XXget_conversationXX", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("GET_CONVERSATION", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = None

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(None)

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = None
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() and {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = None
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get(None, [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", None)
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get([])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", )
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get(None, {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", None).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get({}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", ).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("XXconversation_partsXX", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("CONVERSATION_PARTS", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("XXconversation_partsXX", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("CONVERSATION_PARTS", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "XXsuccessXX": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "SUCCESS": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": False,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "XXconversation_idXX": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "CONVERSATION_ID": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get(None, conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", None),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get(conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", ),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("XXidXX", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("ID", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "XXstateXX": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "STATE": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get(None, ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", None),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get(""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("XXstateXX", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("STATE", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", "XXXX"),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "XXopenXX": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "OPEN": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get(None, False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", None),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get(False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", ),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("XXopenXX", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("OPEN", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", True),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "XXreadXX": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "READ": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get(None, False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", None),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get(False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", ),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("XXreadXX", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("READ", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", True),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "XXsubjectXX": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "SUBJECT": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get(None, ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", None),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get(""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("XXtitleXX", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("TITLE", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", "XXXX"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "XXcreated_atXX": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "CREATED_AT": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get(None, ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", None),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get(""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("XXcreated_atXX", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("CREATED_AT", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", "XXXX"),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "XXupdated_atXX": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "UPDATED_AT": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get(None, ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", None),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get(""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("XXupdated_atXX", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("UPDATED_AT", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", "XXXX"),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "XXsourceXX": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "SOURCE": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get(None, {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", None),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get({}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", ),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("XXsourceXX", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("SOURCE", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "XXcontactsXX": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "CONTACTS": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get(None, {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", None),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get({}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", ),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("XXcontactsXX", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("CONTACTS", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "XXassigneeXX": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "ASSIGNEE": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get(None, {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", None),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get({}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", ),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("XXassigneeXX", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("ASSIGNEE", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "XXtagsXX": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "TAGS": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get(None, {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", None),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get({}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", ),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("XXtagsXX", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("TAGS", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "XXmessagesXX": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "MESSAGES": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "XXidXX": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "ID": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get(None, ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", None),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get(""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("XXidXX", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("ID", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", "XXXX"),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "XXpart_typeXX": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "PART_TYPE": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get(None, ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", None),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get(""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("XXpart_typeXX", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("PART_TYPE", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", "XXXX"),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "XXbodyXX": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "BODY": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get(None, ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", None),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get(""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("XXbodyXX", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("BODY", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", "XXXX"),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "XXcreated_atXX": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "CREATED_AT": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get(None, ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", None),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get(""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("XXcreated_atXX", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("CREATED_AT", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", "XXXX"),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "XXauthorXX": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "AUTHOR": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get(None, {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", None),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get({}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", ),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("XXauthorXX", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("AUTHOR", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "XXtotal_partsXX": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "TOTAL_PARTS": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_206(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_207(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_208(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_209(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_210(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_211(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_212(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_213(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_214(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_215(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_216(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_217(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_218(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_219(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_220(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_221(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_222(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_223(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_224(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_225(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_226(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_227(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_get_conversation__mutmut_228(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una conversacion.

        Args:
            params: Debe contener 'conversation_id'
        """
        conversation_id = params.get("conversation_id", "")
        if not conversation_id:
            return {"success": False, "error": "Parametro requerido: conversation_id"}

        self._log_operation("get_conversation", f"id={conversation_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/conversations/{conversation_id}")

            if response.ok:
                data = response.json() or {}
                conversation_parts = data.get("conversation_parts", {}).get("conversation_parts", [])
                return {
                    "success": True,
                    "conversation_id": data.get("id", conversation_id),
                    "state": data.get("state", ""),
                    "open": data.get("open", False),
                    "read": data.get("read", False),
                    "subject": data.get("title", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "source": data.get("source", {}),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                    "tags": data.get("tags", {}),
                    "messages": [
                        {
                            "id": part.get("id", ""),
                            "part_type": part.get("part_type", ""),
                            "body": part.get("body", ""),
                            "created_at": part.get("created_at", ""),
                            "author": part.get("author", {}),
                        }
                        for part in conversation_parts
                    ],
                    "total_parts": len(conversation_parts),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁ_create_contact__mutmut)
    def _create_contact(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = None
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get(None, "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", None)
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", )
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("XXemailXX", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("EMAIL", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "XXXX")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"XXsuccessXX": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"SUCCESS": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": True, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "XXerrorXX": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "ERROR": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "XXParametro requerido: emailXX"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "PARAMETRO REQUERIDO: EMAIL"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation(None, f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation(f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("XXcreate_contactXX", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("CREATE_CONTACT", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = None
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"XXemailXX": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"EMAIL": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get(None):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("XXnameXX"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("NAME"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = None
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["XXnameXX"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["NAME"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["XXnameXX"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["NAME"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get(None):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("XXphoneXX"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("PHONE"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = None
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["XXphoneXX"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["PHONE"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["XXphoneXX"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["PHONE"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get(None):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("XXcustom_attributesXX"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("CUSTOM_ATTRIBUTES"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = None
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["XXcustom_attributesXX"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["CUSTOM_ATTRIBUTES"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["XXcustom_attributesXX"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["CUSTOM_ATTRIBUTES"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get(None):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("XXroleXX"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("ROLE"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = None

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["XXroleXX"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["ROLE"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["XXroleXX"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["ROLE"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = None

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post(None, json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=None)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post(json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("XX/contactsXX", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/CONTACTS", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = None
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() and {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "XXsuccessXX": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "SUCCESS": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": False,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "XXcontact_idXX": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "CONTACT_ID": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get(None, ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", None),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get(""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("XXidXX", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("ID", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", "XXXX"),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "XXemailXX": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "EMAIL": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get(None, email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", None),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get(email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", ),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("XXemailXX", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("EMAIL", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "XXnameXX": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "NAME": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get(None, ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", None),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get(""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("XXnameXX", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("NAME", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", "XXXX"),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "XXphoneXX": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "PHONE": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get(None, ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", None),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get(""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("XXphoneXX", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("PHONE", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", "XXXX"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "XXcreated_atXX": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "CREATED_AT": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get(None, ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", None),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get(""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("XXcreated_atXX", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("CREATED_AT", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", "XXXX"),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "XXupdated_atXX": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "UPDATED_AT": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get(None, ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", None),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get(""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("XXupdated_atXX", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("UPDATED_AT", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", "XXXX"),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "XXexternal_idXX": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "EXTERNAL_ID": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get(None, ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", None),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get(""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("XXexternal_idXX", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("EXTERNAL_ID", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", "XXXX"),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "XXerror_codeXX": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "ERROR_CODE": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get(None, "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", None) if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", ) if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[1].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("XXcodeXX", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("CODE", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "XXXX") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "XXXX",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_contact__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un contacto en Intercom.

        Args:
            params: Debe contener 'email' y opcionalmente 'name', 'phone'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("create_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"email": email}
            if params.get("name"):
                payload["name"] = params["name"]
            if params.get("phone"):
                payload["phone"] = params["phone"]
            if params.get("custom_attributes"):
                payload["custom_attributes"] = params["custom_attributes"]
            if params.get("role"):
                payload["role"] = params["role"]

            response = self._http.post("/contacts", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "contact_id": data.get("id", ""),
                    "email": data.get("email", email),
                    "name": data.get("name", ""),
                    "phone": data.get("phone", ""),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "external_id": data.get("external_id", ""),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁ_list_contacts__mutmut)
    def _list_contacts(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = None
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get(None, 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", None)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get(20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", )
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("XXlimitXX", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("LIMIT", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 21)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation(None, f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation(f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("XXlist_contactsXX", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("LIST_CONTACTS", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = None
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"XXper_pageXX": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"PER_PAGE": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(None, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, None)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, )}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 61)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get(None):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("XXcreated_afterXX"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("CREATED_AFTER"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = None
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["XXcreated_at_afterXX"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["CREATED_AT_AFTER"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["XXcreated_afterXX"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["CREATED_AFTER"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get(None):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("XXemailXX"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("EMAIL"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = None
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["XXemailXX"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["EMAIL"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["XXemailXX"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["EMAIL"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get(None):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("XXstart_afterXX"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("START_AFTER"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = None

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["XXstarting_afterXX"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["STARTING_AFTER"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["XXstart_afterXX"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["START_AFTER"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = None

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get(None, params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=None)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get(params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", )

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("XX/contactsXX", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/CONTACTS", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = None
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() and {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = None
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get(None, data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", None)
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get(data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", )
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("XXcontactsXX", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("CONTACTS", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get(None, []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", None))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get([]))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", ))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("XXdataXX", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("DATA", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "XXsuccessXX": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "SUCCESS": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": False,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "XXcontactsXX": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "CONTACTS": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "XXidXX": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "ID": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get(None, ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", None),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get(""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("XXidXX", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("ID", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", "XXXX"),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "XXemailXX": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "EMAIL": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get(None, ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", None),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get(""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("XXemailXX", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("EMAIL", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", "XXXX"),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "XXnameXX": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "NAME": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get(None, ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", None),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get(""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("XXnameXX", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("NAME", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", "XXXX"),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "XXphoneXX": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "PHONE": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get(None, ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", None),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get(""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("XXphoneXX", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("PHONE", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", "XXXX"),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "XXcreated_atXX": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "CREATED_AT": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get(None, ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", None),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get(""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("XXcreated_atXX", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("CREATED_AT", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", "XXXX"),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "XXupdated_atXX": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "UPDATED_AT": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get(None, ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", None),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get(""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("XXupdated_atXX", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("UPDATED_AT", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", "XXXX"),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "XXlast_seen_atXX": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "LAST_SEEN_AT": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get(None, ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", None),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get(""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("XXlast_seen_atXX", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("LAST_SEEN_AT", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", "XXXX"),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "XXsigned_up_atXX": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "SIGNED_UP_AT": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get(None, ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", None),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get(""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("XXsigned_up_atXX", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("SIGNED_UP_AT", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", "XXXX"),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "XXtotalXX": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "TOTAL": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "XXpagesXX": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "PAGES": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get(None, {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", None),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get({}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", ),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("XXpagesXX", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("PAGES", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_list_contacts__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de Intercom.

        Args:
            params: Opcionalmente 'limit' y 'created_after'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"per_page": min(limit, 60)}
            if params.get("created_after"):
                query_params["created_at_after"] = params["created_after"]
            if params.get("email"):
                query_params["email"] = params["email"]
            if params.get("start_after"):
                query_params["starting_after"] = params["start_after"]

            response = self._http.get("/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("contacts", data.get("data", []))
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "name": c.get("name", ""),
                            "phone": c.get("phone", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                            "last_seen_at": c.get("last_seen_at", ""),
                            "signed_up_at": c.get("signed_up_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "pages": data.get("pages", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁIntercomConnectorǁ_create_ticket__mutmut)
    def _create_ticket(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = None
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get(None, "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", None)
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", )
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("XXcontact_idXX", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("CONTACT_ID", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "XXXX")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = None
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get(None, "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", None)
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", )
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("XXsubjectXX", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("SUBJECT", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "XXXX")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = None
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get(None, "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", None)
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", )
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("XXbodyXX", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("BODY", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "XXXX")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id and not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"SUCCESS": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": True, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "XXerrorXX": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "ERROR": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "XXParametros requeridos: contact_id, subjectXX"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: CONTACT_ID, SUBJECT"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation(None, f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation(f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("XXcreate_ticketXX", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("CREATE_TICKET", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = None
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "XXcontactsXX": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "CONTACTS": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"XXidXX": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"ID": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "XXticket_attributesXX": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "TICKET_ATTRIBUTES": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "XX_default_title_XX": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_DEFAULT_TITLE_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = None
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["XXticket_attributesXX"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["TICKET_ATTRIBUTES"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["XX_default_description_XX"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_DEFAULT_DESCRIPTION_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get(None):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("XXticket_type_idXX"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("TICKET_TYPE_ID"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = None
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["XXticket_type_idXX"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["TICKET_TYPE_ID"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["XXticket_type_idXX"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["TICKET_TYPE_ID"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get(None):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("XXcustom_attributesXX"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("CUSTOM_ATTRIBUTES"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(None)
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["XXticket_attributesXX"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["TICKET_ATTRIBUTES"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["XXcustom_attributesXX"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["CUSTOM_ATTRIBUTES"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get(None):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("XXassignee_idXX"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("ASSIGNEE_ID"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = None

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["XXassignee_idXX"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["ASSIGNEE_ID"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["XXassignee_idXX"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["ASSIGNEE_ID"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = None

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post(None, json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=None)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post(json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("XX/ticketsXX", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/TICKETS", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = None
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() and {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "XXsuccessXX": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "SUCCESS": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": False,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "XXticket_idXX": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "TICKET_ID": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get(None, ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", None),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get(""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("XXidXX", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("ID", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", "XXXX"),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "XXsubjectXX": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "SUBJECT": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "XXstateXX": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "STATE": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get(None, "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", None),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", ),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("XXstateXX", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("STATE", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "XXsubmittedXX"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "SUBMITTED"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "XXcreated_atXX": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "CREATED_AT": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get(None, ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", None),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get(""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("XXcreated_atXX", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("CREATED_AT", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", "XXXX"),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "XXupdated_atXX": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "UPDATED_AT": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get(None, ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", None),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get(""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("XXupdated_atXX", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("UPDATED_AT", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", "XXXX"),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "XXcontactsXX": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "CONTACTS": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get(None, {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", None),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get({}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", ),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("XXcontactsXX", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("CONTACTS", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "XXassigneeXX": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "ASSIGNEE": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get(None, {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", None),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get({}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", ),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("XXassigneeXX", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("ASSIGNEE", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "XXerror_codeXX": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "ERROR_CODE": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get(None, "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", None) if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", ) if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[1].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("XXcodeXX", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("CODE", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "XXXX") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "XXXX",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁIntercomConnectorǁ_create_ticket__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un ticket en Intercom.

        Args:
            params: Debe contener 'contact_id', 'subject' y 'body'
        """
        contact_id = params.get("contact_id", "")
        subject = params.get("subject", "")
        body = params.get("body", "")
        if not contact_id or not subject:
            return {"success": False, "error": "Parametros requeridos: contact_id, subject"}

        self._log_operation("create_ticket", f"contact={contact_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "contacts": {"id": contact_id},
                "ticket_attributes": {
                    "_default_title_": subject,
                },
            }
            if body:
                payload["ticket_attributes"]["_default_description_"] = body
            if params.get("ticket_type_id"):
                payload["ticket_type_id"] = params["ticket_type_id"]
            if params.get("custom_attributes"):
                payload["ticket_attributes"].update(params["custom_attributes"])
            if params.get("assignee_id"):
                payload["assignee_id"] = params["assignee_id"]

            response = self._http.post("/tickets", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "ticket_id": data.get("id", ""),
                    "subject": subject,
                    "state": data.get("state", "submitted"),
                    "created_at": data.get("created_at", ""),
                    "updated_at": data.get("updated_at", ""),
                    "contacts": data.get("contacts", {}),
                    "assignee": data.get("assignee", {}),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                    "error_code": errors[0].get("code", "") if errors else "",
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

mutants_xǁIntercomConnectorǁ__init____mutmut['_mutmut_orig'] = IntercomConnector.xǁIntercomConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁ__init____mutmut['xǁIntercomConnectorǁ__init____mutmut_1'] = IntercomConnector.xǁIntercomConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁ__init____mutmut['xǁIntercomConnectorǁ__init____mutmut_2'] = IntercomConnector.xǁIntercomConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁ__init____mutmut['xǁIntercomConnectorǁ__init____mutmut_3'] = IntercomConnector.xǁIntercomConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁ__init____mutmut['xǁIntercomConnectorǁ__init____mutmut_4'] = IntercomConnector.xǁIntercomConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁ__init____mutmut['xǁIntercomConnectorǁ__init____mutmut_5'] = IntercomConnector.xǁIntercomConnectorǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁ__init____mutmut['xǁIntercomConnectorǁ__init____mutmut_6'] = IntercomConnector.xǁIntercomConnectorǁ__init____mutmut_6 # type: ignore # mutmut generated

mutants_xǁIntercomConnectorǁconnect__mutmut['_mutmut_orig'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_1'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_2'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_3'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_4'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_5'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_6'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_7'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_8'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_9'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_10'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_11'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_12'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_13'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_14'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_14 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_15'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_15 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_16'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_16 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_17'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_17 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_18'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_18 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_19'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_19 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_20'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_20 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_21'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_21 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_22'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_22 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_23'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_23 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_24'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_24 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_25'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_25 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_26'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_26 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_27'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_27 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_28'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_28 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_29'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_29 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_30'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_30 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_31'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_31 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_32'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_32 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_33'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_33 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_34'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_34 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_35'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_35 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_36'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_36 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_37'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_37 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_38'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_38 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_39'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_39 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_40'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_40 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_41'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_41 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_42'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_42 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_43'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_43 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_44'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_44 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_45'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_45 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_46'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_46 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_47'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_47 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_48'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_48 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_49'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_49 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_50'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_50 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_51'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_51 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_52'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_52 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_53'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_53 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_54'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_54 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_55'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_55 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_56'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_56 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_57'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_57 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_58'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_58 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_59'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_59 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_60'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_60 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_61'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_61 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_62'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_62 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_63'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_63 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_64'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_64 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_65'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_65 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_66'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_66 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_67'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_67 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_68'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_68 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_69'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_69 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_70'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_70 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut_71'] = IntercomConnector.xǁIntercomConnectorǁconnect__mutmut_71 # type: ignore # mutmut generated
mutants_xǁIntercomConnectorǁconnect__mutmut['xǁIntercomConnectorǁconnect__mutmut