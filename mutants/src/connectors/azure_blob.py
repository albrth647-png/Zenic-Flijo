"""
Conector Azure Blob Storage — Operaciones de Almacenamiento
==============================================================

Permite subir, descargar, listar y gestionar blobs en
contenedores de Azure Blob Storage usando la REST API.
"""

from __future__ import annotations

import base64
import hashlib
import hmac
import json
import os
import re
from datetime import UTC, datetime
from typing import Any
from urllib.parse import quote

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁAzureBlobConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut: MutantDict = {}  # type: ignore


class AzureBlobConnector(BaseConnector):
    """Conector para Azure Blob Storage: operaciones CRUD en contenedores y blobs.

    Usa la REST API de Azure Blob Storage con autenticacion SharedKey
    o SAS token via HttpClient.
    """

    name = "azure_blob"
    version = "1.0.0"
    description = "Sube, descarga y gestiona blobs en contenedores de Azure Blob Storage"
    category = "cloud_storage"
    icon = "database"
    author = "Zenic-Flijo"

    # Azure Blob Storage API version
    _API_VERSION = "2023-01-03"

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://{account}.blob.core.windows.netXX"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://{ACCOUNT}.BLOB.CORE.WINDOWS.NET"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = ""
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = None
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_6(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = "XXXX"
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_7(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = None
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_8(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = "XXXX"
        self._sas_token: str = ""
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_9(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = None
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_10(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = "XXXX"
        self._connection_string: str = ""

    def xǁAzureBlobConnectorǁ__init____mutmut_11(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = None

    def xǁAzureBlobConnectorǁ__init____mutmut_12(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://{account}.blob.core.windows.net"
        self._http: HttpClient | None = None
        self._account_name: str = ""
        self._account_key: str = ""
        self._sas_token: str = ""
        self._connection_string: str = "XXXX"

    # ── Conexion ────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXAzureBlobConnector: credenciales no configuradasXX")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("azureblobconnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AZUREBLOBCONNECTOR: CREDENCIALES NO CONFIGURADAS")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return True

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = None
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(None, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, None) else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr("get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, ) else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "XXget_credentialsXX") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "GET_CREDENTIALS") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = None
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            None, os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", None
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "XXconnection_stringXX", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "CONNECTION_STRING", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get(None, "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", None)
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", )
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("XXAZURE_STORAGE_CONNECTION_STRINGXX", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("azure_storage_connection_string", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "XXXX")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = None
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            None, os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", None
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "XXaccount_nameXX", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "ACCOUNT_NAME", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get(None, "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", None)
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", )
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("XXAZURE_STORAGE_ACCOUNT_NAMEXX", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("azure_storage_account_name", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "XXXX")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = None
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            None, os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", None
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "XXaccount_keyXX", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "ACCOUNT_KEY", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get(None, "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", None)
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", )
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("XXAZURE_STORAGE_ACCOUNT_KEYXX", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("azure_storage_account_key", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "XXXX")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = None

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            None, os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", None
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "XXsas_tokenXX", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "SAS_TOKEN", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get(None, "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", None)
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", )
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("XXAZURE_STORAGE_SAS_TOKENXX", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("azure_storage_sas_token", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "XXXX")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string or not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(None)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error(None)
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("XXAzureBlobConnector: account_name no proporcionadoXX")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("azureblobconnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AZUREBLOBCONNECTOR: ACCOUNT_NAME NO PROPORCIONADO")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return True

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key or not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error(None)
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("XXAzureBlobConnector: account_key o sas_token no proporcionadosXX")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("azureblobconnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AZUREBLOBCONNECTOR: ACCOUNT_KEY O SAS_TOKEN NO PROPORCIONADOS")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return True

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = None

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = None
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=None,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_93(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=None,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_94(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_95(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_96(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_97(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=31,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_98(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = None

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_99(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header(None, "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_100(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", None)  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_101(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_102(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", )  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_103(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("XXAuthorizationXX", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_104(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_105(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("AUTHORIZATION", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_106(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "XXXX")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_107(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation(None, f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_108(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", None)
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_109(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation(f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_110(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", )
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_111(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("XXconnectXX", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_112(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("CONNECT", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_113(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation(None, f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_114(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", None)

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_115(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation(f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_116(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", )

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_117(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("XXconnectXX", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_118(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("CONNECT", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_119(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = None
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_120(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request(None, "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_121(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", None)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_122(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_123(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", )
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_124(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("XXGETXX", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_125(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("get", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_126(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "XX/?comp=listXX")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_127(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?COMP=LIST")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_128(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = None
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_129(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = False
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_130(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation(None, f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_131(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", None)
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_132(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation(f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_133(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", )
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_134(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("XXconnectXX", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_135(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("CONNECT", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_136(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return False
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_137(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code != 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_138(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 404:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_139(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = None
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_140(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = False
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_141(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation(None, f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_142(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", None)
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_143(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation(f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_144(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", )
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_145(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("XXconnectXX", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_146(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("CONNECT", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_147(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return False
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_148(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(None)
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_149(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return True
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_150(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(None)
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAzureBlobConnectorǁconnect__mutmut_151(self) -> bool:
        """Establece conexion con Azure Blob Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AzureBlobConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._connection_string = creds.get(
            "connection_string", os.environ.get("AZURE_STORAGE_CONNECTION_STRING", "")
        )
        self._account_name = creds.get(
            "account_name", os.environ.get("AZURE_STORAGE_ACCOUNT_NAME", "")
        )
        self._account_key = creds.get(
            "account_key", os.environ.get("AZURE_STORAGE_ACCOUNT_KEY", "")
        )
        self._sas_token = creds.get(
            "sas_token", os.environ.get("AZURE_STORAGE_SAS_TOKEN", "")
        )

        # Parsear connection string si esta disponible
        if self._connection_string and not self._account_name:
            self._parse_connection_string(self._connection_string)

        if not self._account_name:
            logger.error("AzureBlobConnector: account_name no proporcionado")
            return False

        if not self._account_key and not self._sas_token:
            logger.error("AzureBlobConnector: account_key o sas_token no proporcionados")
            return False

        # Construir base_url
        base_url = f"https://{self._account_name}.blob.core.windows.net"

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=base_url,
            connector_name=self.name,
            timeout=30,
        )
        self._base_url = base_url

        # Configurar autenticacion
        if self._sas_token:
            self._http.set_header("Authorization", "")  # SAS va en query params
            self._log_operation("connect", f"SAS token configurado, account={self._account_name}")
        else:
            self._log_operation("connect", f"SharedKey configurado, account={self._account_name}")

        # Verificar conexion listando contenedores
        try:
            resp = self._azure_request("GET", "/?comp=list")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK, account={self._account_name}")
                return True
            # 403 es aceptable — el endpoint responde pero permisos limitados
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"Azure Blob API OK (403 en list containers), account={self._account_name}")
                return True
            logger.error(f"AzureBlobConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AzureBlobConnector: API fallo: {exc}")
            return True

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "XXupload_blobXX": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "UPLOAD_BLOB": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "XXdownload_blobXX": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "DOWNLOAD_BLOB": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "XXlist_blobsXX": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "LIST_BLOBS": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "XXdelete_blobXX": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "DELETE_BLOB": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "XXget_blob_urlXX": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "GET_BLOB_URL": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "XXlist_containersXX": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "LIST_CONTAINERS": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁAzureBlobConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Azure Blob Storage."""
        action_map: dict[str, Any] = {
            "upload_blob": self._upload_blob,
            "download_blob": self._download_blob,
            "list_blobs": self._list_blobs,
            "delete_blob": self._delete_blob,
            "get_blob_url": self._get_blob_url,
            "list_containers": self._list_containers,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que las credenciales de Azure esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁAzureBlobConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que las credenciales de Azure esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁAzureBlobConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que las credenciales de Azure esten configuradas."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁAzureBlobConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que las credenciales de Azure esten configuradas."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con Azure Blob Storage."""
        self._connected = False
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁAzureBlobConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con Azure Blob Storage."""
        self._connected = False
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁAzureBlobConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con Azure Blob Storage."""
        self._connected = None
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁAzureBlobConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con Azure Blob Storage."""
        self._connected = True
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁAzureBlobConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con Azure Blob Storage."""
        self._connected = False
        self._http = ""
        self._log_operation("disconnect")
        return True

    def xǁAzureBlobConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con Azure Blob Storage."""
        self._connected = False
        self._http = None
        self._log_operation(None)
        return True

    def xǁAzureBlobConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con Azure Blob Storage."""
        self._connected = False
        self._http = None
        self._log_operation("XXdisconnectXX")
        return True

    def xǁAzureBlobConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con Azure Blob Storage."""
        self._connected = False
        self._http = None
        self._log_operation("DISCONNECT")
        return True

    def xǁAzureBlobConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con Azure Blob Storage."""
        self._connected = False
        self._http = None
        self._log_operation("disconnect")
        return False

    # ── Acciones ───────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut)
    def _upload_blob(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = None
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get(None, "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", None)
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", )
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("XXcontainerXX", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("CONTAINER", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "XXXX")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = None
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get(None, "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", None)
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", )
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("XXblob_nameXX", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("BLOB_NAME", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "XXXX")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = None
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get(None)
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("XXcontentXX")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("CONTENT")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = None

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get(None)

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("XXfile_pathXX")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("FILE_PATH")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container and not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"SUCCESS": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": True, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "XXerrorXX": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "ERROR": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "XXParametros requeridos: container, blob_nameXX"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: CONTAINER, BLOB_NAME"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None or not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is not None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"XXsuccessXX": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"SUCCESS": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": True, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "XXerrorXX": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "ERROR": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "XXDebe proporcionar 'content' o 'file_path'XX"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "DEBE PROPORCIONAR 'CONTENT' O 'FILE_PATH'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None or file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is not None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(None, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, None) as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open("rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, ) as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "XXrbXX") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "RB") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = None
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"XXsuccessXX": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"SUCCESS": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": True, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "XXerrorXX": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "ERROR": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = None

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode(None)

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("XXutf-8XX")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("UTF-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = None
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get(None, "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", None)
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", )
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("XXcontent_typeXX", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("CONTENT_TYPE", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "XXapplication/octet-streamXX")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "APPLICATION/OCTET-STREAM")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation(None, f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", None)

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation(f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", )

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("XXupload_blobXX", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("UPLOAD_BLOB", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = None
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(None, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe=None)
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, )
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="XXXX")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = None
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                None,
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                None,
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=None,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers=None,
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "XXPUTXX",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "put",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "XXContent-TypeXX": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "content-type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "CONTENT-TYPE": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "XXx-ms-blob-typeXX": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "X-MS-BLOB-TYPE": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "XXBlockBlobXX",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "blockblob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BLOCKBLOB",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "XXContent-LengthXX": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "content-length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "CONTENT-LENGTH": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(None),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok and resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code != 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 202:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = None
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get(None, "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", None)
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", )
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("XXETagXX", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("etag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETAG", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "XXXX")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "XXsuccessXX": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "SUCCESS": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": False,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "XXcontainerXX": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "CONTAINER": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "XXblob_nameXX": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "BLOB_NAME": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "XXetagXX": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "ETAG": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "XXsizeXX": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "SIZE": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAzureBlobConnectorǁ_upload_blob__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un blob a un contenedor de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y 'content' (o 'file_path')
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_blob", f"container={container}, blob={blob_name}, size={len(content)}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(
                "PUT",
                f"/{container}/{encoded_blob}",
                body=content,
                headers={
                    "Content-Type": content_type,
                    "x-ms-blob-type": "BlockBlob",
                    "Content-Length": str(len(content)),
                },
            )
            if resp.ok or resp.status_code == 201:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut)
    def _download_blob(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = None
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get(None, "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", None)
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", )
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("XXcontainerXX", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("CONTAINER", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "XXXX")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = None
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get(None, "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", None)
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", )
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("XXblob_nameXX", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("BLOB_NAME", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "XXXX")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container and not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"SUCCESS": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": True, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "XXerrorXX": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "ERROR": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "XXParametros requeridos: container, blob_nameXX"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: CONTAINER, BLOB_NAME"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation(None, f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", None)

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation(f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", )

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("XXdownload_blobXX", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("DOWNLOAD_BLOB", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = None
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(None, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe=None)
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, )
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="XXXX")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = None
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(None, f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", None)
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", )
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("XXGETXX", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("get", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = None
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b"XXXX"
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "XXsuccessXX": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "SUCCESS": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": False,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "XXcontainerXX": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "CONTAINER": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "XXblob_nameXX": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "BLOB_NAME": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "XXcontentXX": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "CONTENT": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "XXsize_bytesXX": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "SIZE_BYTES": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "XXcontent_typeXX": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "CONTENT_TYPE": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get(None, "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", None),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", ),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("XXContent-TypeXX", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("content-type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("CONTENT-TYPE", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "XXapplication/octet-streamXX"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "APPLICATION/OCTET-STREAM"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "XXetagXX": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "ETAG": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get(None, ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", None),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get(""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("XXETagXX", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("etag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETAG", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", "XXXX"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁAzureBlobConnectorǁ_download_blob__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("download_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("GET", f"/{container}/{encoded_blob}")
            if resp.ok:
                content = resp.raw if resp.raw else b""
                return {
                    "success": True,
                    "container": container,
                    "blob_name": blob_name,
                    "content": content,
                    "size_bytes": len(content),
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                    "etag": resp.headers.get("ETag", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut)
    def _list_blobs(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = None
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get(None, "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", None)
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", )
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("XXcontainerXX", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("CONTAINER", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "XXXX")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = None
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get(None, "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", None)
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", )
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("XXprefixXX", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("PREFIX", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "XXXX")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = None
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get(None, 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", None)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get(5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", )
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("XXlimitXX", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("LIMIT", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5001)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = None

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get(None, "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", None)

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", )

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("XXmarkerXX", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("MARKER", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "XXXX")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"XXsuccessXX": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"SUCCESS": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": True, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "XXerrorXX": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "ERROR": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "XXParametro requerido: containerXX"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "PARAMETRO REQUERIDO: CONTAINER"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation(None, f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", None)

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation(f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", )

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("XXlist_blobsXX", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("LIST_BLOBS", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = None
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query = f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query -= f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(None)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query = f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query -= f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(None)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = None
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request(None, f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", None)
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request(f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", )
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("XXGETXX", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("get", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = None
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(None)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = None
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(None)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = None

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(None, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, None)

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value("NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, )

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "XXNextMarkerXX")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "nextmarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NEXTMARKER")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "XXsuccessXX": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "SUCCESS": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": False,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "XXcontainerXX": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "CONTAINER": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "XXblobsXX": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "BLOBS": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "XXtotalXX": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "TOTAL": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "XXnext_markerXX": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "NEXT_MARKER": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁAzureBlobConnectorǁ_list_blobs__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista blobs en un contenedor de Azure con paginacion.

        Args:
            params: Debe contener 'container' y opcionalmente 'prefix', 'limit', 'marker'
        """
        container = params.get("container", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 5000)
        marker = params.get("marker", "")

        if not container:
            return {"success": False, "error": "Parametro requerido: container"}

        self._log_operation("list_blobs", f"container={container}")

        try:
            query = f"restype=container&comp=list&maxresults={limit}"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/{container}?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            # Parsear XML de respuesta de Azure
            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            blobs = self._parse_blob_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "container": container,
                "blobs": blobs,
                "total": len(blobs),
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut)
    def _delete_blob(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = None
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get(None, "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", None)
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", )
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("XXcontainerXX", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("CONTAINER", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "XXXX")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = None
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get(None, "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", None)
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", )
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("XXblob_nameXX", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("BLOB_NAME", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "XXXX")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container and not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"SUCCESS": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": True, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "XXerrorXX": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "ERROR": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "XXParametros requeridos: container, blob_nameXX"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: CONTAINER, BLOB_NAME"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation(None, f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", None)

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation(f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", )

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("XXdelete_blobXX", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("DELETE_BLOB", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = None
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(None, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe=None)
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, )
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="XXXX")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = None
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(None, f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", None)
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request(f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", )
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("XXDELETEXX", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("delete", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok and resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code not in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (203, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 205):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"XXsuccessXX": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"SUCCESS": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": False, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "XXdeletedXX": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "DELETED": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": False, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "XXcontainerXX": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "CONTAINER": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "XXblob_nameXX": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "BLOB_NAME": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁAzureBlobConnectorǁ_delete_blob__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un blob de un contenedor de Azure.

        Args:
            params: Debe contener 'container' y 'blob_name'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("delete_blob", f"container={container}, blob={blob_name}")

        try:
            encoded_blob = quote(blob_name, safe="")
            resp = self._azure_request("DELETE", f"/{container}/{encoded_blob}")
            if resp.ok or resp.status_code in (202, 204):
                return {"success": True, "deleted": True, "container": container, "blob_name": blob_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut)
    def _get_blob_url(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = None
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get(None, "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", None)
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", )
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("XXcontainerXX", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("CONTAINER", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "XXXX")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = None
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get(None, "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", None)
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", )
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("XXblob_nameXX", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("BLOB_NAME", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "XXXX")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = None
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get(None, 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", None)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get(3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", )
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("XXexpires_inXX", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("EXPIRES_IN", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3601)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = None  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get(None, "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", None)  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", )  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("XXpermissionsXX", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("PERMISSIONS", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "XXrXX")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "R")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container and not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"SUCCESS": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": True, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "XXerrorXX": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "ERROR": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "XXParametros requeridos: container, blob_nameXX"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: CONTAINER, BLOB_NAME"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation(None, f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", None)

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation(f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", )

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("XXget_blob_urlXX", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("GET_BLOB_URL", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = None
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(None, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe=None)
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, )
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="XXXX")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = None

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = None
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"XXsuccessXX": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"SUCCESS": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": False, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "XXurlXX": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "URL": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "XXexpires_inXX": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "EXPIRES_IN": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = None
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(None, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, None, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, None, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, None)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, )
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"XXsuccessXX": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"SUCCESS": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": False, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "XXurlXX": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "URL": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "XXexpires_inXX": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "EXPIRES_IN": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(None)

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"XXsuccessXX": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"SUCCESS": True, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": False, "url": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "XXurlXX": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "URL": base_url, "expires_in": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "XXexpires_inXX": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "EXPIRES_IN": expires_in, "note": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "XXnoteXX": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "NOTE": "URL sin SAS (requiere account_key o sas_token para SAS)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "XXURL sin SAS (requiere account_key o sas_token para SAS)XX"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "url sin sas (requiere account_key o sas_token para sas)"}

    def xǁAzureBlobConnectorǁ_get_blob_url__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL SAS para acceder a un blob de Azure.

        Args:
            params: Debe contener 'container', 'blob_name' y opcionalmente 'expires_in', 'permissions'
        """
        container = params.get("container", "")
        blob_name = params.get("blob_name", "")
        expires_in = params.get("expires_in", 3600)
        permissions = params.get("permissions", "r")  # read por defecto

        if not container or not blob_name:
            return {"success": False, "error": "Parametros requeridos: container, blob_name"}

        self._log_operation("get_blob_url", f"container={container}, blob={blob_name}")

        encoded_blob = quote(blob_name, safe="")
        base_url = f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}"

        # Si tenemos SAS token, usarlo
        if self._sas_token:
            url = f"{base_url}?{self._sas_token}"
            return {"success": True, "url": url, "expires_in": expires_in}

        # Si tenemos account key, generar SAS signature
        if self._account_key:
            try:
                sas_url = self._generate_sas_url(container, blob_name, permissions, expires_in)
                return {"success": True, "url": sas_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"AzureBlobConnector: error generando SAS URL: {exc}")

        # Fallback: URL directa (solo funciona para blobs publicos)
        return {"success": True, "url": base_url, "expires_in": expires_in, "note": "URL SIN SAS (REQUIERE ACCOUNT_KEY O SAS_TOKEN PARA SAS)"}

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut)
    def _list_containers(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = None
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get(None, "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", None)
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", )
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("XXprefixXX", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("PREFIX", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "XXXX")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = None

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get(None, "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", None)

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", )

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("XXmarkerXX", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("MARKER", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "XXXX")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation(None)

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("XXlist_containersXX")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("LIST_CONTAINERS")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = None
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "XXcomp=listXX"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "COMP=LIST"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query = f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query -= f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(None)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query = f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query -= f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(None)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = None
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request(None, f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", None)
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request(f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", )
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("XXGETXX", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("get", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = None
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(None)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = None
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(None)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = None

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(None, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, None)

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value("NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, )

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "XXNextMarkerXX")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "nextmarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NEXTMARKER")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "XXsuccessXX": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "SUCCESS": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": False,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "XXcontainersXX": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "CONTAINERS": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "XXnext_markerXX": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "NEXT_MARKER": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁAzureBlobConnectorǁ_list_containers__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los contenedores de la cuenta de Azure Storage con paginacion.

        Args:
            params: Opcionalmente 'prefix', 'marker'
        """
        prefix = params.get("prefix", "")
        marker = params.get("marker", "")

        self._log_operation("list_containers")

        try:
            query = "comp=list"
            if prefix:
                query += f"&prefix={quote(prefix)}"
            if marker:
                query += f"&marker={quote(marker)}"

            resp = self._azure_request("GET", f"/?{query}")
            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body_str = resp.body if isinstance(resp.body, str) else str(resp.body)
            containers = self._parse_container_list_xml(body_str)
            next_marker = self._extract_xml_value(body_str, "NextMarker")

            return {
                "success": True,
                "containers": containers,
                "next_marker": next_marker,
            }
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    # ── Azure REST API con SharedKey ────────────────────────────

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut)
    def _azure_request(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_orig(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_1(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = None
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_2(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(None)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_3(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = None

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_4(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime(None)

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_5(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("XX%a, %d %b %Y %H:%M:%S GMTXX")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_6(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %y %h:%m:%s gmt")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_7(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%A, %D %B %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_8(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is not None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_9(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = None

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_10(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b"XXXX"

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_11(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = None
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_12(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(None) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_13(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "XX0XX"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_14(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get(None, "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_15(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", None)

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_16(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_17(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", )

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_18(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("XXContent-TypeXX", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_19(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("content-type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_20(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("CONTENT-TYPE", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_21(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "XXXX")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_22(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = None
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_23(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "XXx-ms-dateXX": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_24(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "X-MS-DATE": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_25(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "XXx-ms-versionXX": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_26(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "X-MS-VERSION": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_27(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "XXContent-LengthXX": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_28(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "content-length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_29(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "CONTENT-LENGTH": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_30(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(None)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_31(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token or "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_32(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "XX?XX" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_33(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_34(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = None
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_35(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token or "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_36(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "XX?XX" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_37(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" not in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_38(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = None

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_39(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key or not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_40(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_41(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = None
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_42(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                None, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_43(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, None, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_44(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, None, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_45(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, None
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_46(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_47(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_48(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_49(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_50(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = None

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_51(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["XXAuthorizationXX"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_52(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_53(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["AUTHORIZATION"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_54(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = None

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_55(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = None

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_56(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "XXmethodXX": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_57(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "METHOD": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_58(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "XXurlXX": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_59(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "URL": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_60(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "XXheadersXX": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_61(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "HEADERS": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_62(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "XXdataXX": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_63(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "DATA": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_64(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "XXtimeoutXX": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_65(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "TIMEOUT": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_66(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 31,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_67(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "XXverifyXX": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_68(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "VERIFY": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_69(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": False,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_70(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = None

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_71(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = None
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_72(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = None

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_73(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=None,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_74(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=None,
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_75(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=None,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_76(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=None,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_77(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=None,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_78(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=None,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_79(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=None,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_80(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_81(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_82(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_83(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_84(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_85(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_86(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_87(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(None),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_88(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(None, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_89(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, None) else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_90(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr("total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_91(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, ) else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_92(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "XXtotal_secondsXX") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_93(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "TOTAL_SECONDS") else 0,
            url=url,
            method=method,
        )

    # ── Azure REST API con SharedKey ────────────────────────────

    def xǁAzureBlobConnectorǁ_azure_request__mutmut_94(
        self,
        method: str,
        path: str,
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP a Azure Blob Storage con autenticacion SharedKey.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse
        """
        import requests as req_lib

        from src.sdk.http_client import HTTPResponse

        now = datetime.now(UTC)
        date_str = now.strftime("%a, %d %b %Y %H:%M:%S GMT")

        # Preparar body
        if body is None:
            body = b""

        content_length = str(len(body)) if body else "0"
        if headers:
            headers.get("Content-Type", "")

        # Construir headers base
        request_headers: dict[str, str] = {
            "x-ms-date": date_str,
            "x-ms-version": self._API_VERSION,
            "Content-Length": content_length,
        }
        if headers:
            request_headers.update(headers)

        # Si tenemos SAS token, agregarlo al query string
        if self._sas_token and "?" not in path:
            path = f"{path}?{self._sas_token}"
        elif self._sas_token and "?" in path:
            path = f"{path}&{self._sas_token}"

        # Si tenemos account key, firmar con SharedKey
        if self._account_key and not self._sas_token:
            authorization = self._compute_shared_key_signature(
                method, path, request_headers, body
            )
            request_headers["Authorization"] = authorization

        # Construir URL
        url = f"https://{self._account_name}.blob.core.windows.net{path}"

        # Ejecutar peticion
        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": request_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 1,
            url=url,
            method=method,
        )

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut)
    def _compute_shared_key_signature(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_orig(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_1(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = None
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_2(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get(None, "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_3(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", None)
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_4(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_5(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", )
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_6(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("XXContent-EncodingXX", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_7(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("content-encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_8(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("CONTENT-ENCODING", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_9(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "XXXX")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_10(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = None
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_11(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get(None, "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_12(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", None)
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_13(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_14(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", )
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_15(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("XXContent-LanguageXX", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_16(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("content-language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_17(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("CONTENT-LANGUAGE", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_18(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "XXXX")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_19(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = None
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_20(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get(None, "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_21(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", None)
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_22(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_23(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", )
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_24(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("XXContent-LengthXX", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_25(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("content-length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_26(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("CONTENT-LENGTH", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_27(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "XXXX")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_28(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") or content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_29(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method not in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_30(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("XXGETXX", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_31(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("get", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_32(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "XXDELETEXX", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_33(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "delete", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_34(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "XXHEADXX") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_35(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "head") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_36(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length != "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_37(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "XX0XX":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_38(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = None
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_39(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = "XXXX"
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_40(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = None
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_41(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get(None, "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_42(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", None)
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_43(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_44(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", )
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_45(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("XXContent-MD5XX", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_46(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("content-md5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_47(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("CONTENT-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_48(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "XXXX")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_49(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = None
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_50(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get(None, "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_51(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", None)
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_52(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_53(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", )
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_54(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("XXContent-TypeXX", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_55(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("content-type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_56(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("CONTENT-TYPE", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_57(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "XXXX")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_58(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = None
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_59(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = "XXXX"
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_60(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = None
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_61(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get(None, "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_62(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", None)
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_63(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_64(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", )
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_65(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("XXIf-Modified-SinceXX", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_66(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("if-modified-since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_67(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("IF-MODIFIED-SINCE", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_68(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "XXXX")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_69(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = None
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_70(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get(None, "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_71(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", None)
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_72(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_73(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", )
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_74(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("XXIf-Unmodified-SinceXX", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_75(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("if-unmodified-since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_76(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("IF-UNMODIFIED-SINCE", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_77(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "XXXX")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_78(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = None

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_79(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get(None, "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_80(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", None)

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_81(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_82(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", )

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_83(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("XXRangeXX", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_84(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_85(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("RANGE", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_86(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "XXXX")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_87(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = None
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_88(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            None
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_89(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.upper(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_90(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith(None)]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_91(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.upper().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_92(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("XXx-ms-XX")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_93(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("X-MS-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_94(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = None

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_95(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) - "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_96(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(None) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_97(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "XX\nXX".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_98(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "XX\nXX"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_99(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = None

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_100(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split(None)[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_101(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("XX?XX")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_102(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[1]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_103(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = None

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_104(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name - path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_105(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" - self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_106(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str - "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_107(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" - ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_108(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) - "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_109(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join(None) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_110(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "XX\nXX".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_111(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "XX\nXX" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_112(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "XX/XX" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_113(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = None
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_114(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(None)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_115(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = None
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_116(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            None,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_117(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            None,
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_118(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            None,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_119(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_120(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_121(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_122(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode(None),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_123(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("XXutf-8XX"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_124(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("UTF-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_125(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = None

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_126(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode(None)

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_127(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(None).decode("utf-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_128(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("XXutf-8XX")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    def xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_129(
        self,
        method: str,
        path: str,
        headers: dict[str, str],
        body: bytes,
    ) -> str:
        """Computa la firma SharedKey para Azure Blob Storage REST API.

        Args:
            method: Metodo HTTP
            path: Ruta del recurso
            headers: Headers de la peticion
            body: Cuerpo de la peticion

        Returns:
            String de autorizacion "SharedKey {account}:{signature}"
        """
        # Construir string to sign segun especificacion Azure
        content_encoding = headers.get("Content-Encoding", "")
        content_language = headers.get("Content-Language", "")
        content_length = headers.get("Content-Length", "")
        # Azure requiere Content-Length vacio para GET/DELETE, sino el valor
        if method in ("GET", "DELETE", "HEAD") and content_length == "0":
            content_length = ""
        content_md5 = headers.get("Content-MD5", "")
        content_type = headers.get("Content-Type", "")
        date = ""
        if_modified_since = headers.get("If-Modified-Since", "")
        if_unmodified_since = headers.get("If-Unmodified-Since", "")
        range_header = headers.get("Range", "")

        # Recopilar x-ms-headers en orden lexicografico
        ms_headers = sorted(
            [(k.lower(), v) for k, v in headers.items() if k.lower().startswith("x-ms-")]
        )
        ms_headers_str = "\n".join(f"{k}:{v}" for k, v in ms_headers) + "\n"

        # Extraer ruta sin query params
        path_only = path.split("?")[0]

        string_to_sign = "\n".join([
            method,
            content_encoding,
            content_language,
            content_length,
            content_md5,
            content_type,
            date,
            if_modified_since,
            if_unmodified_since,
            range_header,
        ]) + "\n" + ms_headers_str + "/" + self._account_name + path_only

        # Firmar con HMAC-SHA256
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("UTF-8")

        return f"SharedKey {self._account_name}:{encoded_signature}"

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut)
    def _generate_sas_url(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_orig(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_1(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = None
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_2(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) - timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_3(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(None) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_4(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=None)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_5(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = None

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_6(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime(None)

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_7(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("XX%Y-%m-%dT%H:%M:%SZXX")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_8(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%y-%m-%dt%h:%m:%sz")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_9(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%M-%DT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_10(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = None

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_11(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = None

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_12(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join(None)

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_13(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "XX\nXX".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_14(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "XXXX",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_15(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "XXXX",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_16(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "XXXX",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_17(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "XXXX",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_18(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "XXXX",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_19(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "XXXX",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_20(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "XXXX",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_21(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = None
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_22(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(None)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_23(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = None
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_24(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            None,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_25(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            None,
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_26(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            None,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_27(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_28(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_29(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_30(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode(None),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_31(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("XXutf-8XX"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_32(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("UTF-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_33(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = None

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_34(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode(None)

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_35(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(None).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_36(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("XXutf-8XX")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_37(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("UTF-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_38(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = None
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_39(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(None, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_40(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe=None)
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_41(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_42(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, )
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_43(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="XXXX")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_44(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = None

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_45(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(None)}"
            f"&sig={quote(encoded_signature)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    def xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_46(
        self, container: str, blob_name: str, permissions: str, expires_in: int
    ) -> str:
        """Genera una URL con SAS token para un blob.

        Args:
            container: Nombre del contenedor
            blob_name: Nombre del blob
            permissions: Permisos (r, w, d, etc.)
            expires_in: Tiempo de expiracion en segundos

        Returns:
            URL con SAS token
        """
        from datetime import timedelta

        expiry_time = datetime.now(UTC) + timedelta(seconds=expires_in)
        expiry_str = expiry_time.strftime("%Y-%m-%dT%H:%M:%SZ")

        # Canonicalized resource
        canonical_resource = f"/blob/{self._account_name}/{container}/{blob_name}"

        # String to sign
        string_to_sign = "\n".join([
            permissions,   # signed permissions
            "",            # signed start (optional)
            expiry_str,    # signed expiry
            canonical_resource,  # canonical resource
            "",            # signed identifier
            "",            # signed IP
            "",            # signed protocol
            self._API_VERSION,  # signed version
            "",            # signed resource type
            "",            # signed snapshot time
            "",            # signed encryption scope
        ])

        # Firmar
        decoded_key = base64.b64decode(self._account_key)
        signature = hmac.new(
            decoded_key,
            string_to_sign.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        encoded_signature = base64.b64encode(signature).decode("utf-8")

        # Construir URL
        encoded_blob = quote(blob_name, safe="")
        sas_params = (
            f"sv={self._API_VERSION}"
            f"&sr=b"
            f"&sp={permissions}"
            f"&se={quote(expiry_str)}"
            f"&sig={quote(None)}"
        )

        return f"https://{self._account_name}.blob.core.windows.net/{container}/{encoded_blob}?{sas_params}"

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut)
    def _extract_xml_value(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(pattern, xml_str, re.DOTALL)
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_orig(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(pattern, xml_str, re.DOTALL)
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_1(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = None
        match = re.search(pattern, xml_str, re.DOTALL)
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_2(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = None
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_3(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(None, xml_str, re.DOTALL)
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_4(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(pattern, None, re.DOTALL)
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_5(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(pattern, xml_str, None)
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_6(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(xml_str, re.DOTALL)
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_7(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(pattern, re.DOTALL)
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_8(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(pattern, xml_str, )
        return match.group(1) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_9(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(pattern, xml_str, re.DOTALL)
        return match.group(None) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_10(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(pattern, xml_str, re.DOTALL)
        return match.group(2) if match else ""

    # ── Helpers XML ─────────────────────────────────────────────

    @staticmethod
    def xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_11(xml_str: str, tag: str) -> str:
        """Extrae el valor de un tag XML de la respuesta de Azure.

        Args:
            xml_str: String XML
            tag: Nombre del tag

        Returns:
            Valor del tag o string vacio
        """
        pattern = f"<{tag}>(.*?)</{tag}>"
        match = re.search(pattern, xml_str, re.DOTALL)
        return match.group(1) if match else "XXXX"

    @staticmethod
    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut)
    def _parse_blob_list_xml(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_orig(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_1(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = None
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_2(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = None
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_3(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(None, re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_4(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", None)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_5(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_6(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", )
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_7(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"XX<Blob>(.*?)</Blob>XX", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_8(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<blob>(.*?)</blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_9(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<BLOB>(.*?)</BLOB>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_10(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(None):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_11(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = None
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_12(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(None)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_13(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(2)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_14(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = None

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_15(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = None
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_16(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(None, blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_17(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", None)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_18(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_19(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", )
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_20(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"XX<Name>(.*?)</Name>XX", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_21(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<name>(.*?)</name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_22(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<NAME>(.*?)</NAME>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_23(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = None

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_24(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["XXnameXX"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_25(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["NAME"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_26(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(None)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_27(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(2)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_28(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = None
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_29(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(None, blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_30(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", None, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_31(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, None)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_32(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_33(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_34(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, )
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_35(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"XX<Properties>(.*?)</Properties>XX", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_36(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<properties>(.*?)</properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_37(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<PROPERTIES>(.*?)</PROPERTIES>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_38(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = None
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_39(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(None)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_40(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(2)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_41(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("XXContent-LengthXX", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_42(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("content-length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_43(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("CONTENT-LENGTH", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_44(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "XXContent-TypeXX", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_45(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "content-type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_46(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "CONTENT-TYPE", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_47(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "XXLast-ModifiedXX", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_48(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "last-modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_49(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "LAST-MODIFIED", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_50(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "XXETagXX", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_51(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "etag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_52(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETAG", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_53(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "XXBlobTypeXX"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_54(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "blobtype"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_55(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BLOBTYPE"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_56(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = None
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_57(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(None, props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_58(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", None)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_59(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_60(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", )
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_61(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = None

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_62(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace(None, "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_63(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", None)] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_64(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_65(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", )] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_66(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.upper().replace("-", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_67(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("XX-XX", "_")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_68(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "XX_XX")] = prop_match.group(1)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_69(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(None)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_70(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(2)

            blobs.append(blob_info)

        return blobs

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_71(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list blobs de Azure.

        Args:
            xml_str: String XML con la lista de blobs

        Returns:
            Lista de diccionarios con informacion de cada blob
        """
        blobs: list[dict[str, str]] = []
        # Extraer cada bloque <Blob>...</Blob>
        blob_pattern = re.compile(r"<Blob>(.*?)</Blob>", re.DOTALL)
        for match in blob_pattern.finditer(xml_str):
            blob_xml = match.group(1)
            blob_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", blob_xml)
            if name_match:
                blob_info["name"] = name_match.group(1)

            # Properties
            props_match = re.search(r"<Properties>(.*?)</Properties>", blob_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Content-Length", "Content-Type", "Last-Modified", "ETag", "BlobType"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        blob_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            blobs.append(None)

        return blobs

    @staticmethod
    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut)
    def _parse_container_list_xml(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_orig(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_1(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = None
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_2(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = None
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_3(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(None, re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_4(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", None)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_5(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_6(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", )
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_7(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"XX<Container>(.*?)</Container>XX", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_8(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<container>(.*?)</container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_9(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<CONTAINER>(.*?)</CONTAINER>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_10(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(None):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_11(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = None
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_12(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(None)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_13(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(2)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_14(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = None

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_15(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = None
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_16(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(None, container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_17(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", None)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_18(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_19(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", )
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_20(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"XX<Name>(.*?)</Name>XX", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_21(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<name>(.*?)</name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_22(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<NAME>(.*?)</NAME>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_23(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = None

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_24(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["XXnameXX"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_25(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["NAME"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_26(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(None)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_27(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(2)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_28(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = None
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_29(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(None, container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_30(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", None, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_31(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, None)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_32(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_33(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_34(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, )
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_35(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"XX<Properties>(.*?)</Properties>XX", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_36(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<properties>(.*?)</properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_37(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<PROPERTIES>(.*?)</PROPERTIES>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_38(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = None
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_39(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(None)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_40(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(2)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_41(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("XXLast-ModifiedXX", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_42(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("last-modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_43(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("LAST-MODIFIED", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_44(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "XXETagXX", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_45(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "etag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_46(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETAG", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_47(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "XXPublicAccessXX"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_48(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "publicaccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_49(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PUBLICACCESS"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_50(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = None
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_51(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(None, props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_52(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", None)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_53(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_54(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", )
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_55(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = None

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_56(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace(None, "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_57(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", None)] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_58(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_59(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", )] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_60(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.upper().replace("-", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_61(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("XX-XX", "_")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_62(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "XX_XX")] = prop_match.group(1)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_63(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(None)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_64(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(2)

            containers.append(container_info)

        return containers

    @staticmethod
    def xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_65(xml_str: str) -> list[dict[str, str]]:
        """Parsea la respuesta XML de list containers de Azure.

        Args:
            xml_str: String XML con la lista de contenedores

        Returns:
            Lista de diccionarios con informacion de cada contenedor
        """
        containers: list[dict[str, str]] = []
        container_pattern = re.compile(r"<Container>(.*?)</Container>", re.DOTALL)
        for match in container_pattern.finditer(xml_str):
            container_xml = match.group(1)
            container_info: dict[str, str] = {}

            name_match = re.search(r"<Name>(.*?)</Name>", container_xml)
            if name_match:
                container_info["name"] = name_match.group(1)

            props_match = re.search(r"<Properties>(.*?)</Properties>", container_xml, re.DOTALL)
            if props_match:
                props = props_match.group(1)
                for prop_tag in ("Last-Modified", "ETag", "PublicAccess"):
                    prop_match = re.search(f"<{prop_tag}>(.*?)</{prop_tag}>", props)
                    if prop_match:
                        container_info[prop_tag.lower().replace("-", "_")] = prop_match.group(1)

            containers.append(None)

        return containers

    @_mutmut_mutated(mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut)
    def _parse_connection_string(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_orig(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_1(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = None
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_2(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(None)
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_3(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split("XX;XX")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_4(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = None
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_5(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "XX=XX" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_6(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_7(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                break
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_8(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = None
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_9(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition(None)
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_10(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.rpartition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_11(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("XX=XX")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_12(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = None
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_13(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.upper().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_14(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = None
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_15(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key != "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_16(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "XXaccountnameXX":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_17(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "ACCOUNTNAME":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_18(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = None
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_19(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key != "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_20(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "XXaccountkeyXX":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_21(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "ACCOUNTKEY":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_22(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = None
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_23(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key != "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_24(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "XXsastokenXX":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_25(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "SASTOKEN":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_26(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = None
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_27(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key != "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_28(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "XXblobendpointXX":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_29(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "BLOBENDPOINT":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_30(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = None
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_31(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(None, value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_32(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", None)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_33(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_34(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", )
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_35(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"XXhttps://([^.]+)\.blob\.core\.windows\.netXX", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_36(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"HTTPS://([^.]+)\.BLOB\.CORE\.WINDOWS\.NET", value)
                if match and not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_37(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match or not self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_38(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and self._account_name:
                    self._account_name = match.group(1)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_39(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = None

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_40(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(None)

    def xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_41(self, conn_str: str) -> None:
        """Parsea una connection string de Azure Storage.

        Args:
            conn_str: Connection string de Azure Storage
        """
        parts = conn_str.split(";")
        for part in parts:
            part = part.strip()
            if "=" not in part:
                continue
            key, _, value = part.partition("=")
            key = key.lower().strip()
            value = value.strip()
            if key == "accountname":
                self._account_name = value
            elif key == "accountkey":
                self._account_key = value
            elif key == "sastoken":
                self._sas_token = value
            elif key == "blobendpoint":
                # Extraer account name del endpoint
                match = re.match(r"https://([^.]+)\.blob\.core\.windows\.net", value)
                if match and not self._account_name:
                    self._account_name = match.group(2)

mutants_xǁAzureBlobConnectorǁ__init____mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ__init____mutmut['xǁAzureBlobConnectorǁ__init____mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ__init____mutmut_12 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁconnect__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_67'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_68'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_69'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_70'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_71'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_72'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_73'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_74'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_75'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_76'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_77'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_78'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_79'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_80'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_81'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_82'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_83'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_84'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_85'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_86'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_87'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_88'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_89'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_90'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_91'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_92'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_93'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_94'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_95'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_96'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_96 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_97'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_97 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_98'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_98 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_99'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_99 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_100'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_100 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_101'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_101 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_102'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_102 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_103'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_103 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_104'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_104 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_105'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_105 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_106'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_106 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_107'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_107 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_108'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_108 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_109'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_109 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_110'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_110 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_111'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_111 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_112'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_112 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_113'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_113 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_114'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_114 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_115'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_115 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_116'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_116 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_117'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_117 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_118'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_118 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_119'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_119 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_120'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_120 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_121'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_121 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_122'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_122 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_123'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_123 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_124'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_124 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_125'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_125 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_126'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_126 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_127'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_127 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_128'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_128 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_129'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_129 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_130'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_130 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_131'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_131 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_132'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_132 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_133'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_133 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_134'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_134 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_135'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_135 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_136'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_136 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_137'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_137 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_138'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_138 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_139'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_139 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_140'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_140 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_141'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_141 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_142'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_142 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_143'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_143 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_144'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_144 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_145'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_145 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_146'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_146 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_147'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_147 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_148'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_148 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_149'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_149 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_150'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_150 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁconnect__mutmut['xǁAzureBlobConnectorǁconnect__mutmut_151'] = AzureBlobConnector.xǁAzureBlobConnectorǁconnect__mutmut_151 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁexecute__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁexecute__mutmut['xǁAzureBlobConnectorǁexecute__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁexecute__mutmut_22 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁvalidate__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁvalidate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁvalidate__mutmut['xǁAzureBlobConnectorǁvalidate__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁvalidate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁvalidate__mutmut['xǁAzureBlobConnectorǁvalidate__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁvalidate__mutmut_2 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁdisconnect__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁdisconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁdisconnect__mutmut['xǁAzureBlobConnectorǁdisconnect__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁdisconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁdisconnect__mutmut['xǁAzureBlobConnectorǁdisconnect__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁdisconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁdisconnect__mutmut['xǁAzureBlobConnectorǁdisconnect__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁdisconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁdisconnect__mutmut['xǁAzureBlobConnectorǁdisconnect__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁdisconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁdisconnect__mutmut['xǁAzureBlobConnectorǁdisconnect__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁdisconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁdisconnect__mutmut['xǁAzureBlobConnectorǁdisconnect__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁdisconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁdisconnect__mutmut['xǁAzureBlobConnectorǁdisconnect__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁdisconnect__mutmut_7 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_67'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_68'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_69'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_70'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_71'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_72'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_73'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_74'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_75'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_76'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_77'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_78'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_79'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_80'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_81'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_82'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_83'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_84'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_85'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_86'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_87'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_88'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_89'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_90'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_91'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_92'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_93'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_94'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_95'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_96'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_96 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_97'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_97 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_98'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_98 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_99'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_99 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_100'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_100 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_101'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_101 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_102'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_102 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_103'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_103 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_104'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_104 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_105'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_105 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_106'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_106 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_107'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_107 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_108'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_108 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_109'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_109 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_110'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_110 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_111'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_111 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_112'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_112 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_113'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_113 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_114'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_114 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_115'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_115 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_116'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_116 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_117'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_117 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_118'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_118 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_119'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_119 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_120'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_120 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_121'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_121 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_122'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_122 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_123'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_123 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_124'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_124 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_125'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_125 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_126'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_126 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_127'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_127 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_128'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_128 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_129'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_129 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_130'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_130 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_131'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_131 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_132'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_132 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_133'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_133 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_134'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_134 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_135'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_135 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_136'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_136 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_137'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_137 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_138'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_138 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_139'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_139 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_140'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_140 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_141'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_141 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_upload_blob__mutmut['xǁAzureBlobConnectorǁ_upload_blob__mutmut_142'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_upload_blob__mutmut_142 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_67'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_68'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_69'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_70'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_71'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_72'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_73'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_74'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_75'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_76'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_77'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_78'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_79'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_80'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_81'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_82'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_83'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_84'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_85'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_86'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_87'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_88'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_89'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_90'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_download_blob__mutmut['xǁAzureBlobConnectorǁ_download_blob__mutmut_91'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_download_blob__mutmut_91 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_67'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_68'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_69'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_70'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_71'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_72'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_73'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_74'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_75'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_76'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_77'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_78'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_79'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_80'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_81'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_82'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_83'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_84'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_85'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_86'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_87'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_88'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_89'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_90'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_91'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_92'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_93'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_94'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_95'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_blobs__mutmut['xǁAzureBlobConnectorǁ_list_blobs__mutmut_96'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_blobs__mutmut_96 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_67'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_68'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_69'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_70'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_delete_blob__mutmut['xǁAzureBlobConnectorǁ_delete_blob__mutmut_71'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_delete_blob__mutmut_71 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_67'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_68'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_69'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_70'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_71'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_72'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_73'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_74'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_75'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_76'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_77'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_78'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_79'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_80'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_81'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_82'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_83'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_84'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_85'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_86'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_87'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_88'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_89'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_90'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_91'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_92'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_93'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_get_blob_url__mutmut['xǁAzureBlobConnectorǁ_get_blob_url__mutmut_94'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_get_blob_url__mutmut_94 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_list_containers__mutmut['xǁAzureBlobConnectorǁ_list_containers__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_list_containers__mutmut_66 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_67'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_68'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_69'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_70'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_71'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_72'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_73'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_74'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_75'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_76'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_77'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_78'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_79'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_80'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_81'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_82'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_83'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_84'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_85'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_86'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_87'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_88'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_89'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_90'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_91'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_92'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_93'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_azure_request__mutmut['xǁAzureBlobConnectorǁ_azure_request__mutmut_94'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_azure_request__mutmut_94 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_67'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_68'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_69'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_70'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_71'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_72'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_73'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_74'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_75'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_76'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_77'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_78'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_79'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_80'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_81'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_82'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_83'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_84'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_85'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_86'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_87'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_88'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_89'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_90'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_91'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_92'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_93'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_94'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_95'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_96'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_96 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_97'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_97 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_98'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_98 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_99'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_99 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_100'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_100 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_101'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_101 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_102'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_102 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_103'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_103 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_104'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_104 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_105'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_105 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_106'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_106 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_107'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_107 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_108'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_108 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_109'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_109 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_110'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_110 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_111'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_111 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_112'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_112 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_113'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_113 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_114'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_114 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_115'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_115 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_116'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_116 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_117'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_117 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_118'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_118 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_119'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_119 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_120'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_120 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_121'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_121 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_122'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_122 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_123'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_123 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_124'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_124 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_125'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_125 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_126'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_126 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_127'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_127 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_128'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_128 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut['xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_129'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_compute_shared_key_signature__mutmut_129 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_generate_sas_url__mutmut['xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_generate_sas_url__mutmut_46 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_extract_xml_value__mutmut['xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_extract_xml_value__mutmut_11 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_66'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_67'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_68'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_69'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_70'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_71'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_blob_list_xml__mutmut_71 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_42'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_43'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_44'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_45'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_46'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_47'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_48'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_49'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_50'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_51'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_52'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_53'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_54'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_55'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_56'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_57'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_58'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_59'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_60'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_61'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_62'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_63'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_64'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut['xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_65'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_container_list_xml__mutmut_65 # type: ignore # mutmut generated

mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['_mutmut_orig'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_1'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_2'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_3'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_4'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_5'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_6'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_7'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_8'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_9'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_10'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_11'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_12'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_13'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_14'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_15'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_16'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_17'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_18'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_19'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_20'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_21'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_22'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_23'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_24'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_25'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_26'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_27'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_28'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_29'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_30'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_31'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_32'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_33'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_34'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_35'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_36'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_37'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_38'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_39'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_40'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAzureBlobConnectorǁ_parse_connection_string__mutmut['xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_41'] = AzureBlobConnector.xǁAzureBlobConnectorǁ_parse_connection_string__mutmut_41 # type: ignore # mutmut generated


AZURE_BLOB_SCHEMA = ConnectorSchema(
    name="azure_blob",
    version="1.0.0",
    description="Sube, descarga y gestiona blobs en contenedores de Azure Blob Storage",
    category="cloud_storage",
    icon="database",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="upload_blob", description="Sube un blob", category="write"),
        ActionDefinition(name="download_blob", description="Descarga un blob", category="read"),
        ActionDefinition(name="list_blobs", description="Lista blobs en contenedor", category="read"),
        ActionDefinition(name="delete_blob", description="Elimina un blob", category="delete"),
        ActionDefinition(name="get_blob_url", description="Genera URL SAS", category="read"),
        ActionDefinition(name="list_containers", description="Lista contenedores", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="api_key", required_fields=["connection_string"], description="Azure Storage Connection String")
    ],
)
